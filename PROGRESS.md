# PortMate 当前进度与下一阶段目标

审查日期：2026-07-30

本文档对照 [PLAN.md](./PLAN.md) 的最终目标、[README.md](./README.md) 的当前说明、以及当前源码实现，单独记录 PortMate 的实际完成度、缺口和下一阶段目标。

## 审查范围

本次审查覆盖当前仓库内的桌面端、共享核心库、MCP bridge 和项目说明：

- 桌面前端：`src/App.tsx`、`src/api.ts`、`src/types.ts`、`src/styles.css`、`src/sync-input-state.ts`。
- Tauri 后端：`src-tauri/src/app_data_migration.rs`、`src-tauri/src/lib.rs`、`src-tauri/src/state.rs`、`src-tauri/src/session_commands.rs`、`src-tauri/src/session_profile_delete.rs`、`src-tauri/src/session_terminal.rs`、`src-tauri/src/proxy_protocol.rs`、`src-tauri/src/remote_copy.rs`、`src-tauri/src/serial_capture.rs`、`src-tauri/src/sftp_paths.rs`、`src-tauri/src/sftp_transfer.rs`、`src-tauri/src/ssh_auxiliary.rs`、`src-tauri/src/ssh_health.rs`、`src-tauri/src/ssh_security.rs`、`src-tauri/src/telnet_protocol.rs`、`src-tauri/src/tmux_protocol.rs`、`src-tauri/src/modem_protocol.rs`、`src-tauri/src/modem_xmodem.rs`、`src-tauri/src/modem_ymodem.rs`、`src-tauri/src/modem_zmodem.rs`、`src-tauri/Cargo.toml`。
- 共享核心：`crates/portmate-core/src/models.rs`、`store.rs`、`host_keys.rs`、`mcp.rs`、`triggers.rs`、`redaction.rs`。
- MCP stdio bridge：`crates/portmate-mcp/src/main.rs`。
- 项目目标和使用说明：`PLAN.md`、`README.md`、`package.json`、workspace `Cargo.toml`。

判断标准是“源码中是否已有真实可执行路径”。只有 UI 菜单或数据模型存在但未接入真实行为的能力，在本文中按“部分实现”或“未完成”记录。

## 总体结论

PortMate 当前已经从“规划原型”推进到“可运行的 alpha 桌面终端工作台”。核心链路已经成立：

- Tauri v2 + React/TypeScript + Rust 桌面应用可构建运行。
- `@xterm/xterm` 已固定为 `6.0.0`，Unicode 11、Serialize、Clipboard 和 WebGL 兼容插件已按验证版本固定。
- SSH、Shell PTY、Serial、Telnet、Raw TCP、Tmux attach/list/session-window-pane lifecycle/layout/sync、SFTP/SCP、X/Y/ZModem、SSH tunnel、Sysmon、触发器、MCP stdio bridge 都已有实际实现。
- SSH host key 已实现 profile 级隔离，不写系统 `known_hosts`，能覆盖“同 IP/端口不同设备/私钥”的核心场景。
- 私钥、可选密码/私钥口令、MCP live IPC token 已接入 OS keyring，SQLite/文件只保存 `secretRef` 或 `tokenRef`。

但它还不是完整 WindTerm/Bitvise 替代品。当前主要差距集中在：Microsoft Active Directory GSSAPI 实证、portable-vault/keyring 的系统化原生跨平台故障矩阵、真实 macOS/Windows/FreeBSD SSH 主机与物理串口证据、MCP SDK 新稳定版发布后的兼容适配，以及发布签名/安装后 smoke test。仓库已经补齐可在 Linux Docker/Chrome 自动完成的 vttest、全屏程序、Tmux 多版本、SSH/SFTP/SCP、Telnet/TCP、SSH 健康故障、三个 OpenSSH 发行版加 Apache MINA 的 MIT Kerberos GSSAPI、Ubuntu OpenSSH + Samba AD-compatible KDC，以及 TypeScript/Python/Go/Rust/Ruby/Java/Kotlin/C#/Swift MCP SDK 矩阵，并新增三平台 native CI 定义。

## 当前实现快照

### 前端桌面工作台

状态：部分完成，可用但仍是 alpha UI。

已实现：

- 终端优先主界面：四组任务菜单、单层 view 标签和精简状态栏；资源、文件、历史、发送按左/右/底部区域停靠，不同区域可同时显示；同一区的多个工具以标签保留，只有活动工具展开，标签栏支持同区重排和跨区拖放，三向边界支持拖动/键盘调整、双击复位和尺寸持久化，关闭全部可选面板后终端恢复全宽。
- 所有主要设置入口使用弹窗，而不是右侧抽屉。
- 新建会话、保存、保存并连接、关闭连接、重连入口已接通。
- 不同协议有不同设置分组：Shell、SSH、Tmux、Telnet、Tcp、Serial。
- `xterm.js` 终端渲染已接入 FitAddon、SearchAddon、WebLinksAddon、Unicode11Addon、SerializeAddon、ClipboardAddon 和按需加载的 WebglAddon。`终端 -> 查找`、`Ctrl/Cmd+F` 和 WindTerm 终端默认键 `Ctrl+Shift+F` 会在当前焦点 pane 打开增量查找条，支持前后匹配、计数、大小写、整词和正则模式；`终端 -> 跳转到行` 已从错误的日志搜索路由改为当前焦点 XTerm buffer 的本地绝对/相对行跳转，输入实时居中预览，Escape 恢复原 viewport，Enter 提交且不向远端发送文本，越界目标拒绝。Profile 终端设置提供 PortMate 深色、石墨、Solarized 深色和 PortMate 浅色四套受控主题；字体、字号、滚屏和主题保存后会原位更新同一 Profile 的已挂载 view，不重建 XTerm 或丢失 buffer，未知旧主题在前后端都回退默认值。行/列/滚屏/字号分别限制为 `1..=512`、`1..=1024`、`0..=10,000,000`、`6..=72`；TERM 限制为 64 字节标准名称字符，字体族限制 256 个 Unicode 字符并拒绝控制字符，前端保存/XTerm 创建与后端加载/保存均执行同一边界。桌面保存事务完成后会主动向独立 Tauri 窗口广播已提交的 SessionSummary；浏览器预览通过 session cache storage 事件同步，1.2 秒轮询继续作为 runtime 状态兜底。独立窗口除实时事件监听外，还以 2 秒周期无重叠读取最新 600 条日志，监听注册竞态或事件投递间隙会自动追赶且由同一事件 ID 去重；会话、日志或 OneKey 轮询暂时失败会保留最后一次有效状态，日志与 OneKey 成功响应独立应用。WebGL 初始化失败或 context loss 会回退 DOM renderer；Linux Tauri WebKitGTK 固定使用 DOM renderer，避免 VMware 上 WebGL 上下文未报错但终端像素全黑。Linux 启动会在 GTK 初始化前检测同用户 Fcitx5，仅当输入法变量为空或残留为 IBus 时修复为一致的 Fcitx 环境，保留其他显式配置；OSC 52 只允许写系统剪贴板，远端读取固定返回空内容。
- 主窗口 session 列表只以后端 Store 为权威，不再用 localStorage cache 恢复业务状态；它写入的 v1 SessionSummary cache 只供独立终端和串口分析器在后端刷新前显示首帧。读取端兼容旧数组，但会完整校验 Profile、Runtime、六种连接、嵌套 Host Key/Identity/Jump/Tunnel/Trigger 结构和唯一 ID；未知版本、合法 JSON 中的缺字段、Profile/Runtime 不一致、重复 ID、语法损坏或存储拒绝均整体降级为空缓存，不会使主窗口或独立工具白屏。工作区、快捷键、Quick Commands、同步输入和终端偏好写入也改为 best-effort，localStorage 被策略或配额拒绝时保留当前进程内状态且不会从 effect/保存动作抛错。Chrome 工作区矩阵用无 Tauri 的三个真实路由和强制 `setItem` 失败锁定这些恢复行为。
- 分屏布局使用 v4 递归树，每个 view 有独立 ID、可选别名/颜色和 session binding；同一 session 可在同组拥有多个视图，复制/重命名/着色不会新建连接或修改 Profile，关闭/恢复、精确排序、跨组定点拖放、四方向拆分、整组合并及独立窗口往返都按 view ID 处理。每个 pane 是最多 32 个 view 的有序分组，拖放显示前/后插入位置，跨组目标满载时保持原树不变；WindTerm 2.7 的 12 种预设颜色、复制、移动、拆分、关闭/恢复和独立窗口入口统一归属 view 右键菜单，pane 标题栏只保留连接和协议相关控制。v1 平铺 snapshot、v2 单视图树、v3 session-list 分组和更早 localStorage key 会原位迁移并修复重复 node/view ID。布局支持任意组合的水平/垂直嵌套、关闭叶子后自动折叠父 split，以及最多 16 pane/8 层的明确边界；splitter 可鼠标/触控拖动、方向键调节、Home/End 跳到 15/85% 边界并双击复位，比例会随 group view 列表、active pane/view 和标签颜色一起持久化。WindTerm 2.7 默认键位中的 `Alt+方向键` 几何焦点导航、`Alt+-`/`Alt+\` 向下/向右拆分、Shift 变体向上/向左拆分、`Alt+X` 关闭 active pane 和 `Alt+Z` pane zoom 已接入，并只在 XTerm 工作区持有键盘焦点时拦截；active pane 与真实 XTerm textarea 焦点保持同步。终端设置的快捷键命令表支持一段或两段组合、显式禁用、恢复默认和冲突阻断。独立窗口只有创建成功才更新原树，并保留 view ID、别名、颜色和同一后端 session；浏览器预览使用同源 popup 与校验后的 postMessage。zoom 隐藏 sibling branch/splitter 但保持其他 XTerm 挂载，方向焦点连同 zoom 一起移动；邻居交换继续由工作区快捷键提供。profile 列表变化后会剔除失效 session view、收敛空分支，同一 session 的多 view 只产生一个启动连接目标。
- WindTerm `Split View To Group` 的上/下/左/右四个方向已收进 view 右键菜单的单个“移到新分组”子菜单；只会从至少含两个 view 的来源分组移出精确命中的 view，达到 16 pane/8 层边界时保持原树不变。
- pane 标签右键菜单和工作区快捷键支持关闭活动/其他/右侧 view，以及当前进程内最近 32 条有界关闭历史的重新打开；关闭 view 不断开后端 session，空 group 自动折叠，最后一个工作区 view 受保护，原 group 消失时恢复到活动非满 group。顶层 session 右键菜单已改为明确的“断开会话”语义。
- WindTerm `Terminal.Save` / `SaveSelectionAs` 对应的终端文本导出已接入终端菜单和 pane 标签右键菜单，并按 session ID + view ID + 焦点状态只读取目标 XTerm。完整 buffer 导出基于 active buffer 的解析后 cell 文本，去除控制序列、拼接 `isWrapped` 物理行、保留中间空逻辑行并移除尾部空行；selection 保留精确空格和换行，空 buffer/selection 会在前端阻断。两条路径按 UTF-8 字节执行 16 MiB 双端上限且不触发任何终端输入。桌面端只允许已保存 session，复验有界且无控制字符的 session/view ID，把文本原子写入 app data 的 `exports/` 并生成 SHA-256 sidecar，Unix 文件权限为 `0600`；浏览器预览按需加载 Blob 下载 helper。导出正文不进入日志、SQLite 或 session event。
- 终端右键菜单和快捷键的复制、全选和清除已从页面级 `document.execCommand`/DOM selection 改为按 session ID + view ID + 焦点状态路由到唯一 XTerm；空选区复制明确报错，同 session 的 Primary/Mirror 不会混淆。Remote/Local 接入 WindTerm `Ctrl+Shift+C` 复制和 `Ctrl+Shift+A` 全选，Normal/Command 保留本地编辑语义，repeat 不重复写剪贴板。原先只有状态栏提示和边框的“块选择”现在会把左键拖拽转换为 XTerm 原生 Alt 列选择；远端启用 mouse reporting 时同时使用 XTerm force-selection modifier，矩形选择不会生成任何终端鼠标/键盘输入。清除选择只清除 XTerm range，不会意外关闭块选择模式。
- 终端正文右键菜单已按 WindTerm `terminal/configs/menu.config` 接入复制、粘贴、查找、在线搜索、选择、导出、触发器和三个本地 buffer 动作；菜单按实时 selection/normal/alternate buffer 状态启用，并通过 session ID + view ID + 焦点状态只操作命中的 XTerm。在线搜索优先读取目标 XTerm 精确选区，无选区才回退同一 session 的最近行，按 2,048 Unicode 字符设界并用结构化 Google URL + `noopener,noreferrer` 打开，不再误取页面 DOM selection。清除回滚使用 `CSI 3 J`，清除屏幕使用 `CSI 2 J` + cursor home，清除全部组合两者；alternate screen 禁用后两项。Remote/Local 的 `Ctrl+L` 和 `Ctrl+Shift+L` 直接走相同本地解析；这些本地/外链动作均不调用 `send_text`、`send_bytes` 或 `run_command`。
- WindTerm 锁屏已从占位入口改为真实状态：状态栏按钮和主/独立终端内的 `Ctrl+Alt+L`/macOS `Meta+Alt+L` 共用从首帧起不透明、焦点封闭的全屏遮罩，不断开会话或停止输出；安全设置可启用启动锁屏和默认 30 分钟、边界 `1..=1440` 分钟的空闲锁屏。只含原因/时间的 v1 本地 marker 让刷新、重启和 detached window 都保持遮罩，存在但损坏的 marker 会保持锁定并由主窗口修复；独立窗口会禁用终端输入并返回主窗口解锁。存在 Portable Vault 时会先锁定 Stronghold、用主密码验证并恢复锁前 provider 状态，当前窗口会话内的刷新也保留该恢复状态；错误消息不暴露后端路径。未配置 vault 或浏览器预览时明确降级为无认证的隐私遮罩。
- WindTerm FreeType 风格自由输入已接入 `终端 -> 自由输入`：只有焦点 pane 打开本地编辑器，草稿按 Unicode 字符限制为 32,768，Enter 原子提交、Shift+Enter 换行、Escape 取消、Ctrl/Meta+Shift+X 剪切选区；提交时统一终端回车并追加一次执行回车，会话切换会清理未提交草稿。自由输入与终端查找互斥，不触发工作区快捷键，并在启用同步输入时复用目标过滤、协议换行、延迟及批量前后缀。
- WindTerm Quick Commands 已接入 `工具 -> 快速命令` 管理器与 `工作区 -> 快捷栏`：支持最多 64 条命令的增删改、上下排序、插入文本/按目标协议终止符执行两种模式和显式保存/取消；名称与命令正文分别按 Unicode 字符限制为 64/8,192，v1 localStorage 会迁移旧 `{name,text}` 数组并修复非法/重复 ID。调用复用同步输入的原子 FIFO、协议换行、目标、延迟及批量前后缀，执行型命令写入有界历史并走显式 `run_command`；Quick Commands 不进入加密凭据 provider，禁止保存密码、token 或私钥。
- `终端设置 -> 命令历史` 已从空表单补为真实策略：旧字符串数组迁移为带毫秒时间戳的 v2 快照，保存开关、`1..10,000` 条容量、`0..3,650` 天保留期和清除按钮共同驱动主窗口历史面板及主/独立窗口补全。重复命令更新到首位，单项限制 8,192 Unicode 字符，持久化 UTF-8 总量限制 2 MiB；关闭磁盘保存会删除快照但保留当前进程历史，清除同时清空两者。历史引擎按需加载，避免重新推高主包。
- WindTerm scheme 风格命令补全已接入焦点 Insert（内部兼容旧 Remote 值）模式的 Shell/SSH/Tmux/Telnet/Raw TCP 主窗口与独立窗口。候选在本地合并常用命令名、选项、子命令、显式历史和 Quick Commands，不探测远端或调用外部服务；`自动补全`设置会独立控制来源、1/2/3 字符触发、5/7/10 行列表和预览方式。候选区固定放在输入内容下方，窄 pane 最多占 45% 高度；空间不足时只在裁剪容器内视觉上移完整 XTerm，不改变 terminal host 尺寸或远端 PTY 行列，因此既不覆盖正在输入的命令，也不会因候选开关扰动 Vim/Tmux。方向键选择、Tab 只经现有交互/同步输入 lane 追加尚未输入的后缀、Escape 关闭，绝不自动执行命令。跟踪器支持纯追加输入、Backspace、Ctrl+U/Ctrl+W；bracketed paste、浏览器/系统普通 paste、光标/未知控制序列、引号、重定向和 shell 运算符都会保守暂停到下一行，OneKey 敏感提示出现时也会先抑制候选，避免伪造远端行编辑状态。真实 Chrome 终端回归锁定普通 paste 后候选消失及下一次回车后的恢复、候选与终端内容的像素无重叠边界，并强制在底行打开候选验证 host 高度、XTerm rows/cols 和后端 resize 调用保持不变。
- WindTerm OneKeys 已接入 `工具 -> OneKeys` 和 `Ctrl+T Ctrl+K`/macOS `Meta+T Meta+K`：Account/SSH 凭据按最多 64 条管理，支持用户名、密码、私钥口令、自动/native/Portable Stronghold Secret 存储、兼容会话绑定、显式保存/删除，以及向当前已连接且已绑定的会话手动发送用户名/密码/口令。SSH OneKey 还可从已绑定 SSH/Tmux Profile 选择可认证的 Profile Vault/System File/Agent 公钥身份，排除 public-key-only；前端只提交来源 Profile ID 与 identity ID，后端重新查找、规范化并克隆身份。持久化摘要只暴露 Secret 是否存在以及身份标签/来源/指纹，不返回 Secret 引用、私钥路径或正文；身份 Secret 纳入全局引用计数。发送走每会话出站 lane，并记录无可读正文的 `one-key` control event。SSH 连接弹窗只列出绑定当前 Profile 的 SSH OneKey，选择后前端仅提交 OneKey ID；后端重新验证类型/绑定并解密用户名、密码和私钥口令，运行时清除 Profile 的旧密码/口令引用，避免两个来源混用；OneKey 自有身份会替换运行时身份列表、启用 identities-only 并确保 public-key 进入认证顺序。未选择自有身份时口令仍可接入 Profile 已配置私钥，密码接入 password/keyboard-interactive。主窗口和独立窗口还会按 WindTerm 默认规则跨分片/ANSI/退格识别当前末行的 username/login/password 提示，过滤密码修改提示，只在聚焦 pane 显示绑定候选确认条；用户输入、提示变化或关闭 `终端设置 -> 自动补全 -> OneKey 终端提示补全` 会撤销。补全请求仅提交 OneKey/会话/字段/提示事件 ID，后端在 Secret 读取前及等待出站 lane 后重新验证提示时效、字段、绑定、用户名和 OneKey 版本，成功只记录关联原提示事件且无正文的 `one-key-completion` control event。OneKeys 与 localStorage Quick Commands 保持独立。
- `工作区` 菜单中的资源管理器、文件管理器、历史命令、发送、快捷栏和状态栏均为带勾选态的真实开关；重复展示同一批 Profile 的右侧会话栏已删除，资源树成为唯一会话浏览面。资源、文件、历史和发送现在使用左/右/底部三处停靠区：不同区域可同时显示；同一区的多个工具以标签保留，非活动内容继续挂载但折叠，标签栏可聚焦、关闭、同区重排或拖到其他区域，空区域也提供边缘落点；三条边界支持 pointer/touch 拖动、方向键 16px 微调、Home/End 和双击内容感知复位。侧栏尺寸限制在 200..720px 且渲染时不超过 38vw，底栏限制在 120..600px 且不超过 45vh，损坏快照不能挤走终端；可见性、顺序、聚焦面板、停靠位置和自定义尺寸使用 v6 面板快照持久化，v1-v5 自动迁移。默认工作区只保留 256px 左侧资源区、pane 单层 view 标签、终端和精简状态栏；侧边文件管理器扩至 360px 并把本地/远端改为上下布局，底部发送区默认只保留发送、文本/Hex 和带已配置提示的高级入口，次数、间隔和目标按需展开，关闭全部可选区域后终端恢复全宽。顶层从八类菜单收敛为会话、终端、工作区、工具四类，搜索保持为桌面/移动端均可达的图标。重复的全局 session 标签、中心连接摘要和 pane 常驻复制/移动/弹出/关闭按钮已删除；连接状态由标签色点表达，标题栏只保留连接/断开及串口 DTR/RTS/BRK，低频 view 操作进入标签右键菜单并按真实结构禁用。状态栏只保留 Sysmon、键盘模式、同步状态、块选择和锁屏。资源树筛选匹配名称、ID、分组、标签、协议、状态和协议端点，历史筛选折叠空白并忽略大小写但仍插入原始命令；筛选都有清除和无结果状态。旧版全开默认会迁移为精简布局并丢弃退休的 sessions 字段；隐藏区域完整归还终端。专注按钮和 `Alt+Enter` 临时隐藏停靠区但不改写持久化选择；同步输入开启时强制保留状态栏风险提示。
- `会话 -> 新建工作区窗口` 可同时打开多个完整工作台；新窗口使用受限 `workspace-*` 路由和 Tauri capability，以级联初始位置显示为可由系统拖动、调整大小的原生窗口，并保持独立的临时 pane/dock 布局，不会读写主工作区快照或重复触发启动连接。独立终端窗格携带来源工作区 ID，连接、锁屏和返回操作只送回该来源窗口；旧 URL/消息仍默认回 `main`，但来源标签只能是 `main` 或规范 `workspace-*`，不会接受任意窗口名。
- 资源树会话右键菜单已提供带明确保留边界的 Profile 删除。排队中/运行中的传输会阻止删除；活动 SSH/Shell/TCP/Telnet/Serial/Tmux、命令和 tunnel 先走完整关闭路径。Store 事务删除 Profile、runtime、内存事件/传输/timeline/sysmon 和 Profile 级 Host Key，项目级信任保留但解除来源 Profile；OneKey 解除会话绑定和来源身份但不擅自删除凭据，MCP scoped grant 删除最后目标时自动撤销而不会因空列表扩成全局。Store 加载也按实际 Profile 集合清理孤儿 runtime/event/transfer/timeline/sysmon 和 Profile 级 Host Key，项目/用户级 key 只解除失效来源，审计记录继续保留；后续复用相同 session ID 不会继承旧状态或信任。两个旧 ID 规范化后碰撞时不会互相覆盖：后项获得稳定且有界的 `:loaded:N` ID，runtime/event/Host Key/grant/OneKey 按精确原始 ID 分流，只有唯一无歧义的规范化引用才回退匹配。系统事件 outbox 只在 Store 提交成功后清理，提交失败不会丢掉原 Profile 的待写诊断；删除后 reader 的迟到系统事件会因 Profile 已不存在而丢弃，不能重新生成孤儿历史。安全审计和磁盘日志分片保留，孤立 Profile secret 按全局引用计数清理；工作区/关闭历史/启动目标/缓存同步收敛，独立 Tauri 窗口收到删除事件后立即停止输入并关闭。
- `npm run desktop:clean` 除清理 snap 注入的 GTK/WebKit 环境外，会读取 Tauri 的实际 dev URL，并在 Linux/macOS/Windows 上通过 OS 监听与进程命令信息识别当前仓库的旧 Vite；端口属于其他程序或无法验证时只报告 PID 并拒绝误杀，监听进程在识别后自行退出产生的 `ESRCH` 则按已停止处理。清理器在所有平台保留 Rust toolchain、编译器、SDK、签名和 PortMate 等无关环境变量；Unix 的 XDG 数据目录优先恢复 snap 注入前的原值，普通非 snap 自定义值原样保留，否则按当前 `HOME` 和系统共享目录构造，不含开发者绝对路径。`1420` 旧 Vite 清理、新 Vite/sidecar/Rust 桌面编译启动和外部 listener 拒绝误杀均已用真实 Linux 链路验证；Windows `netstat` listener、当前/其他 checkout 命令边界、Linux/macOS/Windows 环境保留和退出竞态使用无副作用回归锁定。
- Linux 启动会在 WebKitGTK 初始化前检查 DMI；VMware 虚拟机且用户没有显式覆盖时自动设置 `WEBKIT_DISABLE_DMABUF_RENDERER=1`，规避 Ubuntu 24.04 / WebKitGTK 2.52 的 DMABUF 全白窗口，实体 Linux 与显式 WebKit 配置保持默认行为。默认全白、手工软件渲染和自动检测修复三条路径已在 VMware20,1 上用窗口像素截图验证；`npm run test:linux-desktop-smoke` 还会在临时 XDG 数据根目录中，从无 `WEBKIT_DISABLE_DMABUF_RENDERER` 覆盖的环境启动真实 Tauri/WebKit 窗口，只接受本轮新窗口，通过 XWD 颜色数、亮度方差和全白/全黑比例验证首帧，等待隔离 Store 和 IPC endpoint 就绪，经 `WM_DELETE_WINDOW` 正常关窗，确认应用以 0 退出且 endpoint 被删除，再清理测试数据。Ubuntu Native CI 会在隔离 D-Bus + Xvfb + Openbox 显示中执行同一门禁。
- Vite 开发服务明确忽略 Rust `target/`、参考程序 `ref/` 和构建输出 `dist/`，并对剩余源码使用 250 ms polling；当前 `target/` 超过 7 万文件或 VS Code 等进程已占满 Linux `max_user_instances` 时，不会再因 inotify `ENOSPC` 崩溃并留下空白工作区。项目通过 `.nvmrc` 和 `engines.node` 固定已验证的 Node 22.20.0/最低 22.12.0，错误 Node 版本会在桌面启动前给出明确诊断。
- 发布构建会清理旧 bundle、使用 `0022` umask、规范增量 release binary 与只读资源权限，再生成目标三元组对应的 `portmate-mcp` sidecar，并把主程序、bridge、PNG/ICNS/ICO 标准图标和 Apache-2.0 许可证纳入 Tauri bundle。Linux 构建会保留调用方提供的有效 `LDAI_RUNTIME_FILE`；否则从已有 Tauri AppImage 插件只读解析 type-2 ELF/SquashFS 边界并把 runtime 注入 LinuxDeploy，缓存存在时不再依赖打包阶段访问 GitHub，缓存不存在时仍保留标准下载路径。Linux AppImage 会在复用原 runtime 的前提下重新封装，消除 LinuxDeploy 写入的开发机绝对 `.DirIcon` 并把目录/可执行/普通文件规范为 `0755/0755/0644`。仓库脚本会解包核对 DEB、RPM、AppImage 的主程序、sidecar、desktop entry、图标、许可证、全部文件权限与 symlink 边界，并把精确生产 CSP 和主/独立窗口 capability source policy 与每个包内主程序的嵌入元数据交叉验证；每个包内 bridge 还分别通过官方 SDK 的 stdio 8 消息和 HTTP 9 请求序列。`npm run test:linux-appimage-smoke` 会提取最终 AppImage，在同一隔离 XDG Store 上连续启动两次 packaged `AppRun`，逐次验证真实窗口像素、IPC 发布、`WM_DELETE_WINDOW` 正常退出、endpoint 删除和 IPC credential 轮换，并要求 Store SHA-256 跨无操作重启完全相同。真实 AppImage 已在 VMware20,1 上通过该门禁；Ubuntu Native CI 也已接入 Xvfb + Openbox 路径。MCP HTTP 页返回已安装 bridge 的真实路径、精确 Store 路径和不含 token/keyring ref 的平台 shell 启动命令，不再要求最终用户在源码目录执行 `cargo run`，也不会再因缺少 `PORTMATE_STORE_PATH` 启动一个无法连接桌面的 bridge。bundle identifier 已从与 macOS `.app` 扩展冲突的 `dev.portmate.app` 改为 `dev.portmate.desktop`；首次启动只会把旧数据目录原子迁移到不存在或空的新目录，两个非空目录并存时拒绝猜测合并。生产 WebView 已启用限制 script/connect/frame/object/base/form 的 CSP，开发 HMR 继续使用独立 dev CSP。
- `工作区 -> 还原布局` 会重新读取并应用 snapshot，不再以阻塞成功通知打断下一步操作；启动模式支持不连接、按上次 pane 或按最多四个指定 Profile 顺序连接，自动去重/过滤失效会话并避免凭据弹窗并发覆盖。指定模式的下拉框现在从已保存 session 生成名称/协议标签并持久化真实、有界且无 NUL 的 Profile ID，已删除目标会明确显示为不可用；旧版“最近使用/活动工作区/默认组”等会被解析器丢弃的占位选项已删除。终端设置收敛为应用、安全、快捷键、自动补全、命令历史、鼠标、同步输入七页；无运行时消费的外观、标签、全局终端/文本、代理、小部件和 X Server 页面不再暴露。
- 启动自动连接复用可见连接控件的状态语义：配置目标处于 `Disconnected`/`Blocked`/`Error` 时会重试，`Connecting`/`Connected`/`Reconnecting` 则保留现有 runtime 而不重复 open。Chrome 隔离回归从持久 `Error` 状态只恢复指定的 Local Shell，并确认其他已连接会话没有收到 open。
- 搜索弹窗由桌面/移动端均可达的顶部图标打开，支持会话和已加载日志搜索，并与资源树共享 ID、名称、分组、标签、协议、状态及端点匹配；日志还会匹配所属会话上下文、按全局事件时间倒序、限制 80 条结果和 2,048 字符预览。组合框支持 ArrowUp/ArrowDown 选择、Enter 打开精确 workspace view、Escape 关闭，结果使用稳定 event/session key 和 listbox 语义。
- 会话设置已从照搬 WindTerm 层级的多层大表单收敛为通用身份、终端/日志、触发器、协议感知传输和当前协议连接页：Shell/SSH/Tmux/Telnet/Tcp/Serial 的主连接字段分别只出现一次，SFTP/SCP 与 X/Y/ZModem 共用一页限速/默认目录，SSH/Tmux 只保留已接入 Profile/runtime 的代理、Host Key 验证、Agent、密码引用和公钥能力。Profile ID/会话名称/分组按 256/128/256 Unicode 字符设界，最多保留 32 个去重后的 64 字符标签；前端输入和后端 Store 加载均移除控制字符，标签编辑器保留正在输入的逗号，不再吞掉第二个标签的分隔符。现有 Profile 保存使用 expected/current/incoming 三方合并：调用方未改字段保留并发 resize、凭据迁移或其他窗口的新值，非重叠设置修改可合并，同一叶子/列表的不同修改返回不含字段正文的路径冲突；显式代理密码更新在创建新 Secret 前单独核对预期引用，新建请求也不能借规范化 ID 碰撞覆盖旧 Profile。协议标签加树形侧栏的双层导航已替换为会话类型和配置项两个紧凑选择器；终端设置也改为单层顶部标签和全宽内容。`工具 -> 传输任务` 是唯一顶层传输入口，选择器按 Profile 类型/五个协议开关过滤；后端在排队前和 worker 真正启动前复核同一能力，非 SSH/Tmux 的远端 SFTP/SCP 被拒绝但保留内部纯本地复制兼容，相对本地路径会真正以默认目录解析。Bell、模式、键盘、安全、窗口、选择、数据模式、系统、字符集、Shell 提权、SSH 压缩/算法/MAC/X11、Telnet 本地回显和串口换行等仅写 localStorage 却不生效的伪设置已删除；`portmate.sessionPrefs.*` 状态和“保存为默认设置”入口也已移除。会话设置桌面上限为 760×640，移动端在同一行展示两个选择器并限制内部滚动。
- SessionStore 最多接受 10,000 个 Profile；满额时已有 ID 可更新，新 ID 会在代理 Secret 写入前拒绝并在 Store 事务内复核。所有桌面 Store writer 再次校验总量；桌面 SQLite/旧 JSON 与 standalone MCP 对超限快照 fail-closed 拒绝，不会在 ID remap/normalization 时静默裁掉 Profile 或暴露部分集合。
- MCP Bridge 已拆为互斥的授权、HTTP、审计三个任务页，低频页不会再与授权表单纵向堆叠。授权 Client ID 编辑后保持只读，后端会再次规范化并校验 ID/name、重复 scope、重复/非法 session ID、会话数和 grant 总量。加载旧快照时等价重复 grant 会去重，冲突/非法项会变成已撤销且无 scope 的可见 review 记录，孤儿 session ID 会从 allowedSessions 移除，原本 scoped 且失去全部目标的 grant 会显式撤销；既不会卡死 SQLite mirror，也不会因空列表重开全局授权或在同 ID Profile 重建后复活。无 grant 时非空 client ID 保持默认只读；保存任意 grant 后进入显式策略模式，`read-sessions`/`read-logs` 会按 client 和允许会话过滤 tools/resources/prompts、全局日志搜索、transfer resource 与 HTTP SSE 状态，存量 revoked/到期规则不能授权，删除全部 grant 则显式回到空 Store 默认读模式；live desktop IPC 再次复核并只接受清单内 MCP 命令。grant 可启用 `confirmWrites`，覆盖 `write-input`、`manage-sessions`、`transfer`、`tunnel` 全部写 scope；新建授权默认开启，旧数据缺失字段时兼容为关闭。桌面使用懒加载 alertdialog 做一次性决定，拒绝按钮默认焦点，Escape 拒绝，锁屏期间不能批准；队列按创建时间排序、去重并最多保留 32 项，60 秒超时 fail-closed，已响应 ID 的有界 tombstone 防止迟到 snapshot 复活。UI 事件只携带 approval/client/action/session/scope/时间戳，不含命令、路径、密码、Secret 或原始参数。审计先持久化 `pending-approval`，允许后在副作用前持久化 approved authorization，最后收敛为 succeeded/failed；拒绝、超时和 UI 不可用均明确记为 denied。审计页支持文本、decision、session/global 和 scope 联合筛选，使用时间倒序列表与详情检查器；刷新会重新读取 Store，导出则按当前筛选 ID 从 Store 复取并整批拒绝重复、非法或已消失记录。JSONL 最多 5,000 条、单条 64 KiB、总计 16 MiB，原子落盘并生成 SHA-256 sidecar，Unix 权限为 `0600`。Transfer/Tunnel/Tmux/Trigger 相关入口已存在；Sysmon 已从单行通知升级为 CPU/内存/负载/吞吐概览与进程/磁盘/网络/趋势四标签工作窗口，趋势可切换 CPU/内存利用率与 RX/TX 速率，并提供当前会话可启停、立即采样后每 10 秒刷新的紧凑工具栏 applet。
- Sysmon 还提供当前会话的常驻右侧栏：可独立启停、立即采样并每 10 秒刷新，持续显示 CPU/内存/吞吐摘要和有界趋势入口；桌面/移动 Playwright 已验证停靠、刷新、断线和布局边界。
- 同步输入会把输入按 FIFO 顺序发送到源 pane 和经过协议过滤的已连接 pane；支持按协议换行、0..5000 ms 目标间延迟、显式批量发送各应用一次的受限前后缀、失败/即时取消反馈和明显目标计数。普通 XTerm 键击及原生 bracketed 键盘粘贴保持无前后缀的流式输入，终端菜单、上下文和中键粘贴走批量路径。源会话始终保留，重复 pane binding 只发送一次；设置持久化但开关每次启动默认关闭。
- 发送停靠区、按需展开的发送次数/间隔/目标、命令历史、Hex 字节发送已接通真实后端。
- 终端交互支持选择即复制、右键/中键粘贴；`mouseReporting` 和 `mouseCopyOnSelect` 设置已真实驱动主窗口与独立窗口。关闭鼠标报告时只阻断完整 SGR/URXVT/X10/DEC locator 协议帧，不吞普通 CSI 键盘或 focus 序列；选择仍可用 Shift 强制进入。终端缓存额外保存 SerializeAddon 未覆盖的 1005/1006/1015/1016 鼠标编码，恢复全屏 view 后不会从 SGR/pixel 模式退回 X10。
- WindTerm 键盘模式已按 workspace view 独立持久化并以 Insert 为默认交互态：Insert 使用竖线光标并把按键发往远端，候选打开时首次 `Escape` 只关闭候选，否则 `Escape` 进入使用方块光标的 Normal；Normal 的 `i` 和 `Ctrl+Enter` 返回 Insert，状态栏与独立窗口按钮也直接切换 Insert/Normal。内部继续兼容旧 `remote/local/normal/command` 快照，原本的本地浏览与本地编辑仍可从终端菜单进入，本地编辑使用下划线光标；Local/Normal 支持计数前缀、`h/j/k/l`/方向键、行首尾、word/page/half-page、`gg`/`G`、滚行、查找、字符/整行可视选择和 `y` 复制。所有未映射键、IME 数据及中键粘贴在非 Insert 模式都由 `onData` 二次阻断，不会误发远端；真实 XTerm 回归验证模式切换不发送 Escape/`i` 字节并同步更新实际 cursor style。

主要缺口：

- WindTerm 的 view 精确排序/跨 group 定点拖放和完整 group 合并、逐 view 标签颜色、pane 独立窗口/返回、最多两段的 chord keymap、默认分屏创建、方向焦点移动、关闭、交换、zoom、比例调整和恢复已可用。上一个/下一个标签会按活动 pane 的独立 view ID 顺序首尾循环，不会跳过绑定同一 session 的 view；Remote 默认 `Alt+[`/`Alt+]`，Local/Normal 默认 `Ctrl+PgUp`/`Ctrl+PgDn`，其余模式保留对应按键语义，切换不跨 pane 或创建后端连接。Remote/Local 的 `Ctrl+Shift+W` 关闭当前 view，全模式 `Ctrl+Shift+T` 恢复最近关闭 view；repeat 不会连续修改历史，Normal/Command 的关闭按键保留给本地编辑，最后一个 workspace view 仍受保护。pane 标签右键菜单已补齐复制 session 名称/URL、保存/重连、水平/垂直分屏、精确 view 移组、关闭其他/右侧、恢复和 session 设置；同 session 多 view 的保存/重连保持原 view ID，全局与 view 右键菜单的分屏方向也已统一为水平向右、垂直向下。
- 终端视图切换使用最多 32 个会话、单项 2 MiB、2,000 行 scrollback 的进程内 LRU 序列化缓存，不把屏幕内容写入 localStorage 或磁盘。缓存现在区分“事件已排队”和“XTerm 已完成解析”：快速切换/React StrictMode 不再把空或半成品缓冲与已消费事件 ID 组合保存。事件 ID 达到 4,000 条后按完成顺序淘汰最旧记录，尚未完成 XTerm 解析的写入受保护；不会再整表清空并让下一轮最新 600 条日志重放。同一后端 session 出现在多个 pane 时，只有焦点 pane 可上报 PTY resize；焦点转移会强制按新 owner 当前尺寸重新同步，避免非活动小 pane 覆盖 vim/tmux/top 的行列数。WindTerm 风格自动多色交互命令行覆盖 Serial/Shell/SSH/TCP/Telnet/Tmux，通过 XTerm decoration 只给识别出的 prompt 行的命令、选项、字符串、路径、地址、数字、变量和操作符着色，不改写远端字节、日志、搜索、复制或导出；分析限制为 1,024 个 Unicode 字符且只重建可见逻辑行，备用屏、密码提示和输入同步丢失时会清空并暂停。`npm run test:terminal-compat` 覆盖 alternate screen、cursor addressing、SGR/truecolor、Unicode 宽字符、view 缓存、鼠标、选择、事件去重、搜索、resize owner、语义颜色深浅主题最终像素/禁用/暂停/恢复/原文搜索/出站字节、真实 Vim/less/top 和 6,000 行性能回归；`npm run test:vttest-compat` 另在 Debian bookworm、Ubuntu 24.04、Debian trixie 的三个 vttest 版本上各运行 13 组 VT100/VT102/VT220/ISO-6429/xterm 套件，并在 Debian/Ubuntu/Alpine 容器中通过双向 PTY 实跑 Vim、less、top、dialog。SRM 因依赖不可预测人工按键不自动化，xterm.js 不响应的 DECREQTPARM 也不伪报通过；真正的 macOS/Windows 原生全屏程序证据由 native runner/后续安装 smoke test 承担。
- 终端设置当前只保留有实际消费者的应用、安全、快捷键、自动补全、命令历史、鼠标和同步输入七页；启动策略、空闲/启动锁屏、两段 chord、自动多色交互命令行、补全来源/触发字符/列表高度/预览、历史容量/保留期/持久化、鼠标报告/选择即复制和同步输入变换均会驱动对应运行时。真实 XTerm Playwright 回归除既有鼠标与选择行为外，现还逐项验证关闭补全、3 字符触发、5 行候选上限、输入框预览、全部来源关闭和语义着色开关；后续新增全局偏好仍须先接入实际行为和同层回归，不能只增加 localStorage 字段或表单控件。

### 连接与传输

状态：核心协议可用，深度能力待补。

已实现：

- SSH PTY shell：`russh` 0.62.4 连接、PTY、resize、password/public-key/keyboard-interactive/ssh-agent、profile-vault 私钥、保存密码/口令。该版本已纳入畸形 PTY request 与 Curve25519 KEX/key 导致 panic 的三个上游安全修复（GHSA-cqjc-rmpq-xprq、GHSA-g9hv-x236-4qp3、GHSA-5xvq-cp9x-6p6r）；升级后的 session、SFTP/SCP、Jump Host direct-tcpip、remote forward 和 11 服务端矩阵均已回归。每个 host-key 扫描跳点的完整认证顺序共用 12 秒总预算，每个正式 Jump Host/目标认证共用生产环境 20 秒 setup 预算；目标认证后的 session channel/PTY/环境/agent forwarding/shell/Tmux attach 另共用一个 20 秒总预算。已连接 runtime 的 SFTP 初始化，以及 SCP/remote copy/Tmux control/tunnel 健康/Sysmon 共用的 channel + exec setup 限制为 10 秒，短时辅助命令会把 setup 计入自身总预算。超时或 runtime 注册前 setup 失败会有界断开当前 handle 和已经建立的 Jump Host 链。
- SSH/Tmux/TCP/Telnet 的代理设置已接入真实 transport：每个 Profile 可选择 HTTP CONNECT 或 SOCKS5，并可使用 HTTP Basic 或 SOCKS5 username/password 认证；SOCKS5 使用 domain target，避免在本机解析目标域名。代理目标规范化、凭据解析、TCP 建连、HTTP CONNECT/SOCKS5 客户端握手和 dynamic tunnel SOCKS5 服务端解析、认证边界与响应解析均已提取到独立 `proxy_protocol.rs`；客户端成功响应会严格验证 RFC 1928 保留字段和非空 domain 型绑定地址，服务端整个握手限制为 5 秒且拒绝含控制或空白字符的 domain target，畸形或慢速代理流不能被当成已建连或长期占用任务。代理密码以事务式 Profile 保存流程写入 native keyring/Stronghold，只持久化 `secretRef`，共享引用计数、孤儿清理、迁移 journal 与恢复投影覆盖四类 Profile。SSH host-key 扫描和正式连接走同一路径；存在 Jump Host 时代理只承载第一条物理连接，后续跳点继续通过 `direct-tcpip`。扫描阶段的每个后续跳点/最终目标 channel open 固定限制为 12 秒，正式连接沿用当前 20 秒 setup 超时；russh channel confirmation 超时时会先请求断开所属 Jump Host session，再清理其余链路，避免取消 confirmation future 后遗留孤儿 channel。
- SSH/Tmux 的重连延迟和协议 KeepAlive 已由 Profile 持久化；延迟范围 100-60,000 ms、默认 1,000 ms，等待期间每 100 ms 重读最新 Profile，因此修改延迟或关闭 reconnect 会影响下一次尝试。KeepAlive 可独立开关并配置 1-3,600 秒探测间隔和 0-20 次未响应上限，默认 30/3；`0` 表示持续发送保活但不因保活未响应自动断开。Profile 还可独立保留系统默认、开启或关闭 OS TCP KeepAlive；显式值只应用于直连或第一条 Jump Host 的物理 socket，后续 `direct-tcpip` 通道不伪装为本地 socket。正式会话和整条 Jump Host 链使用同一组 russh client 参数。后台重连每次尝试都会按 session ID 从 store 重新加载并规范化最新 profile；已保存的 endpoint、username、secretRef、identity、Jump Host、host-key 策略与健康参数会用于下一次尝试。握手期间连接配置变化会废弃旧建立结果和旧失败诊断，关闭 reconnect 会终止 worker；runtime ID 代际校验覆盖 tunnel 恢复和 `Connected` 状态提交，避免已关闭 runtime 被旧任务重新标记为已连接。
- 活动 SSH/Tmux runtime 可执行三层健康检查：3 秒 keepalive 往返决定 transport 是否 `unresponsive`，5 秒 exec channel 必须返回独立 marker，可选 SFTP 的初始化、`canonicalize` 与目录读取共用 5 秒总预算；任一 SFTP 阶段失败或静默则标记 `degraded`，全部请求阶段成功才为 `healthy`。russh SFTP 请求越过健康 deadline 时会在仍持有对应互斥 guard 的路径上淘汰该缓存 session，后续操作可建立新 channel，不会复用带有未完成请求的连接。结果携带三段独立 latency/error 并绑定开始时的 runtime ID，检查期间重连或替换会 fail-closed 要求重试。14 类 OpenSSH fault server 健康矩阵真实覆盖容器暂停与服务端强制关闭、exec 拒绝/静默/错误 marker、SFTP subsystem 缺失/拒绝、版本握手/`REALPATH`/`OPENDIR`/`READDIR` 分阶段静默后的新 channel 恢复、SFTP `canonicalize` 失败与目录读取拒绝，以及检查期间 runtime 代际替换；另有 SCP command 拒绝传输故障。
- Shell：跨平台 PTY 基础能力，支持自定义程序、参数、cwd。
- Profile 在 `Connecting`/`Connected`/`Reconnecting` 状态下禁止直接切换协议类型，必须先关闭会话；同协议内的设置仍可保存。Core runtime 会保留真实 `activeTransport` 直到断开或新 transport 确实启动，避免新协议编码、状态和旧 registry 连接交叉。
- 初次连接后端按 session single-flight，并共用全应用 64 个 open permit；取消句柄持有 permit 到成功、失败或取消收尾真正返回。重复 session 在 lifecycle lane 排队前拒绝，饱和在读取 Profile/Secret、写 Store 或触发 transport 前拒绝；OneKey 登录同样先预留再解析凭据。Close/Delete 仍可在等待 lane 前取消对应 open，permit 不会提前释放给新的同 session 请求。
- Serial：端口枚举、波特率、数据位、停止位、校验、流控、DTR/RTS、Break、文本/Hex 字节发送、读写；波特率输入提供 `110..1500000` 的 17 个常用下拉建议，同时保留正整数范围内的设备自定义速率。每 Profile 保留最多 512 帧/1 MiB 的进程内精确 RX/TX 原始字节捕获，侧栏支持方向、Hex、ASCII 筛选、显式清空，以及把当前可见帧原子导出为未脱敏 JSONL + SHA-256 sidecar。捕获快照、Raw 日志历史复用、筛选和 JSONL 原子导出已归入 `serial_capture.rs`，与串口命令入口分离。捕获跨自动重连并在断线后保留，但默认不持久化；单帧超过 64 KiB 时明确显示 captured/original 长度。Profile 可配置 100-60,000 ms 自动重连延迟（默认 1,000 ms），等待期间每 100 ms 重读最新 Profile，因此缩短延迟或关闭 reconnect 会及时生效。可选 1-86,400 秒接收空闲超时默认关闭/60 秒，只观察 RX 且不会向设备注入通用 heartbeat；适合预期持续上报的设备。读错误或空闲超时会保留精确断线原因。每次尝试都会重新加载最新 Profile，端口、线路或健康参数变化会废弃旧尝试并改用新配置，pending/connected 阶段关闭 reconnect 都会收敛到 `Disconnected`；用户关闭或手动重连也会取消旧重连循环。
- Telnet/Raw TCP：socket 模式读写；Telnet 已有增量 IAC 选项协商、Profile TERMINAL-TYPE、方向独立的 BINARY、NAWS 初始/持续 resize、NVT `CR NUL`/CRLF 编解码和 Hex/raw byte IAC 转义。BINARY/NAWS 可按 Profile 关闭，旧 Profile 默认开启；Telnet 和 Raw TCP 均可在会话设置中显式启用 TLS，默认使用系统 CA 和目标 host 校验证书，可配置独立 TLS Server Name，并仅通过明确的不安全开关接受无效证书。TCP 建连与 TLS 握手共用 15 秒 setup deadline；明文和 TLS runtime 共用同一读写、协商、日志及重连路径。协商状态按 runtime 隔离并在重连时重置，协商回复写失败会结束旧 transport 并进入统一断开/重连流程。
- TCP/Telnet：profile 开启 reconnect 后，远端断开会进入 `Reconnecting`，保留可取消的 runtime 占位并按 Profile 延迟（100-60,000 ms，默认 1,000 ms）后台重连；等待期间每 100 ms 检查最新配置，因此修改延迟或关闭 reconnect 会影响下一次尝试。每次连接前重新加载最新 Profile，host/port/协议/代理/TLS/健康参数及终端类型/尺寸变化会废弃旧连接并改用新配置，关闭 reconnect 会移除占位并收敛到 `Disconnected`。socket 的 OS keepalive 开关、idle、probe interval 和 retry 均由 Profile 持久化，默认 30/10/3；平台支持相应参数时无需注入协议字节即可检测半开连接。loopback 回归覆盖自定义/关闭内核 keepalive、远端立即断开、runtime id 轮换、`Connected -> Reconnecting -> Connected`、断线后切换端口与缩短延迟、代理端点切换，以及 pending/connected 两种阶段关闭重连。
- Tmux：远端 `list-sessions`、`list-windows`、`list-panes`、attach/new-session，以及按 `session:window` 读取和切换 `synchronize-panes`。管理弹窗按 window 聚合并排序 pane，展示活动 pane/命令/标题，并支持点击激活 pane、新建 window、重命名 session/window、水平/垂直 split pane、与相邻 pane 交换、四方向按 5 cells 调整、break 为新 window、按左右/上下方向 move 到其他远端 window、五种内置 window layout，以及经对象确认条关闭 session/window/pane；所有操作都等待 SSH exec 成功并重新读取远端快照后再更新，失败不会保留错误的乐观状态。后端只接受固定 mutation action，source/destination target 限制为 1..=256 字符、名称限制为 1..=128 字符、resize amount 限制为 1..=100，文本均拒绝控制字符并经过 shell quoting；带引号/分号的目标或名称不会注入命令，成功 mutation 会写入带 source/destination 的 session system event。每个 session 行的 Radio 工具可独立启动带 runtime generation 的 `tmux -C` watcher；后端按 `(sessionId,target)` 保存，多 target 可在同一 SSH runtime 并存，精确停止和迟到 stop 事件不会清除其他 target，关闭弹窗会逐项停止自己拥有的 watcher，SSH/应用关闭仍有一次全量兜底清理。watcher 退出时 channel Close 最多等待 1 秒，再移除精确 registry 项并发送 stopped 事件。解析器忽略 `%output`/`%extended-output` 正文，未完成行与 stderr 各限制 64 KiB，只把结构事件按 120ms trailing/1s 最大延迟合并后推送给前端静默刷新；刷新及其他 target 的启停不会关闭行内 editor。管理器已拆为独立 lazy chunk。
- Tmux control watcher 每个 SSH session 最多保留 64 个不同 target，全应用 256 个生命周期 permit 从 SSH lookup/setup 前持续持有到 channel 有界收尾、精确 registry 清理和 stopped 事件完成。饱和在打开 channel 前拒绝；setup 后安装会再次复核容量，并发同 key 启动保留先安装的活动 runtime 并有界关闭冗余 channel。Stop 只标记取消，未退出 watcher 不会提前释放 registry 容量；同 key 可替换已取消 generation，但旧 generation 仍占 permit 到实际退出。
- `npm run test:tmux-version-compat` 在 Debian bullseye/bookworm 和 Alpine 3.21 上真实运行 tmux 3.1c、3.3a、3.5a、3.7b，覆盖带空格名称的 session/window/pane 解析、同步状态、layout/mutation、control-mode 通知和共享协议 helper；3.7b 使用固定 SHA-256 的官方 release tarball 源码构建，旧版输出的 Tab 分隔差异已由固定字段分隔符消除。
- 远端文件浏览/属性/变更、SFTP/SCP/remote-copy、Tmux 状态/变更/control watcher、远端 Sysmon 和 remote-tunnel 健康检查另共用全应用 256 个 SSH auxiliary operation permit。资源 lookup、容量 lease、SFTP 缓存锁/初始化和 10 秒总 setup deadline 已集中到 `ssh_auxiliary.rs`，不再由文件传输模块反向拥有跨域限制。permit 在 SSH runtime lookup 和 channel setup 前以 fail-fast 方式获取，并覆盖一个完整高层操作直到 SFTP/channel 有界收尾；饱和不会打开 channel、修改 Tmux watcher/routing registry 或写入 Sysmon/系统事件。Tmux mutation、pane sync 和后续三次状态读取复用同一 lease，不会在远端变更已经成功后因嵌套抢占而返回限流错误。共享 SSH exec capture 在正常 EOF、总超时、stdout/stderr 超限和非零退出路径都会先走有界 Close，再释放对应 lease；非零状态即使先产生了部分 stdout 也不会被解析为成功，`exit-status` 无论在 EOF 前后到达都会被消费。Linux remote-tunnel `/proc` 探测只在命令内部显式容忍可选的 `tcp6` 表缺失，不会放宽其他调用方的失败语义。
- SFTP：原生 subsystem 浏览、上传、下载、远端复制、递归建目录、递归删除。每个协议请求限制为 20 秒；subsystem 初始化完成后的 upload/download/remote-copy 保持单个传输 future，每 100 ms 检查取消，取消时先销毁 future 再关闭 SFTP session，不重启在途 request，也不需要断开所属 SSH 连接。
- Local copy 与 SFTP upload/download/remote-copy 在提升 `.portmate-part` 前严格核对实际复制字节数和开始时源大小；源文件传输中缩短或增长都会失败并保留 part，不会生成假成功的最终文件或上报超过总大小的进度。断点续传前会按 64 KiB 块比较已存 part 与当前源的前缀，只在完全一致时 seek/append，不一致则从 0 截断重写；比较过程同样可取消。前缀短读仅按安全不匹配从 0 重传，其他本地或远端读取错误会如实返回，不再被掩盖成重传。0 字节 local copy 和 SFTP upload 会真正创建空临时文件并正常 finalize，不再误走“已有完整 part”快路径。
- SCP：上传、下载、远端 `cp` 复制。下载的文件头、正文和完成确认，以及受 flow control 的上传/下载写入，均保持同一个 russh I/O future 并每 100 ms 检查取消；连续 60 秒没有数据或写入完成会以空闲超时失败。上传和下载的正常、取消、超时、协议错误与非零状态路径都使用有界 channel 收尾；EOF 后保留 1 秒等待迟到的 `exit-status`。上传把已有 part 先作为候选偏移，客户端回传同长度本地前缀的 SHA-256，远端只在哈希相同后接受偏移；错误、缺失或不完整确认均从 0 截断重传，并在 EOF 前再次确认本地源既未缩短也未增长。远端脚本的大小读取使用 POSIX `wc -c`，哈希按 `sha256sum`、`shasum -a 256`、`sha256 -q` 回退，不再要求 Linux `stat -c`/单一哈希工具。脚本也不再把 GNU 专属 `--` 传给 `head`、`tail`、`mv` 或 `truncate`：以 `-` 开头的相对路径先规范化为 `./…`，因而可在 BSD/macOS 工具上安全工作；XModem 精确长度收尾在 `truncate` 缺失或失败时回退到 `dd`。下载只在 SCP 完成确认和远端成功状态均通过后才把 `.portmate-part` 提升为最终文件；标准 `scp -f` 不支持偏移协商，因此每次下载从字节 0 重写 part，不会把未验证的旧前缀与新后缀拼接。远端命令复制则在接受已有 part 长度前用 `head`/`cmp` 校验其完整源前缀，等长但内容错误或超长的 part 会截断并从 0 重传，不能拼出混合文件。下载协议只从 stdout 解析控制流和文件数据，stderr 独立限制为 64 KiB；文件头和错误行必须在 64 KiB 内完整换行，防止登录提示污染文件或异常对端无界增长协议缓冲。
- X/Y/ZModem：in-band 传输，块级进度与取消已接入；ZModem 使用 `zmodem2`，自动远端传输使用 lrzsz 的 `rx`/`sx`、`rb`/`sb`、`rz`/`sz`，并通过随机 READY/DONE marker 隔离相邻传输尾部字节、在 SSH PTY 上切换 raw TTY。自动远端上传统一写入同目录 `.portmate-part`，协议和 DONE marker 成功后才提交到最终路径；XModem 会先按精确源长度截断 padding，三种协议都以同文件系统原子 rename 收尾。READY 阶段为复杂交互 shell/低性能主机保留独立 30 秒启动窗口，仍每 100 ms 检查取消与断线；X/YModem sender 的块 ACK 维持独立 12 秒 deadline，并会在收到 NAK 或等待 ACK 超时后有界重发数据块和 EOT。
- SSH tunnel：local、remote reverse、dynamic SOCKS5，桌面端可查看当前会话运行中的 tunnel、停止 tunnel、显示 active/total 连接数、双向字节计数和最后错误；2 秒状态读取按对话框代际串行化，关闭/换会话/新建/停止会失效旧响应，慢请求不能回滚当前列表。local/dynamic 使用统一 SSH backend `direct-tcpip` channel；remote reverse 的 listen/cancel 也进入 backend 边界，russh 继续使用服务端 forwarded-channel 回调，Linux 系统 libssh/GSSAPI runtime 则为每个 session 启动一个按目标端口路由的有界 accept loop，三种模式均可在双后端运行。项目内置的 `libssh-rs` fork 已补齐 upstream 绑定缺失的 `ssh_channel_cancel_forward` 安全封装，因此 Stop 会真正撤销远端监听。dynamic SOCKS5 的 method + CONNECT request 使用单个 5 秒总 deadline，半包/慢速客户端会关闭并进入 tunnel 错误指标，domain target 还会拒绝空值、非 UTF-8、控制字符和空白字符。local/dynamic 的 handle lock 与 `direct-tcpip` open、remote reverse 的本地目标连接分别限制为 10 秒；channel open 超时会先请求断开所属 SSH backend，russh 路径清理由已取消确认 future 留下的潜在孤儿 channel，再由既有断线/重连路径收敛。三种模式共用全应用 256 个活动客户端槽位，饱和 local socket 在创建任务前关闭，饱和、无路由或服务端端口超出 `u16` 的 remote-forward channel 走 1 秒有界 Close，不会把端口回绕到其他路由。remote reverse 本地目标连接失败同样使用该有界收尾。local/dynamic/remote 使用端口 0 时会回填实际监听端口；目标失败会记录错误，后续连接成功会清除 degraded 状态，监听器永久退出会从运行 registry 移除并禁用已保存配置；remote forward 每 15 秒通过远端 Linux `/proc/net/tcp`/`ss`、FreeBSD `sockstat`、macOS `lsof` 或成功执行的 `netstat -ltn` 被动核对监听端口，服务端撤销后会重发原 bind request 并记录恢复事件；存在但参数不兼容的探测工具会回退为 unsupported，不会把空输出误判为监听丢失；Stop 在服务端 cancel 拒绝/超时后仍会清理本地路由/runtime 并把 profile 置为 disabled；SSH channel 断开会移除该会话全部旧 tunnel runtime，自动重连成功后从最新 profile 按原 ID、标签和端口逐条恢复 enabled tunnel，单条恢复失败会保留期望状态、记录事件且不阻断会话和其他 tunnel。
- Tunnel Profile 每个最多保留 64 条定义，全应用另有 256 个活动 tunnel runtime 上限；ID/标签最多 128 个 Unicode 字符，host 最多 255 字符且拒绝空白，端口和 mode-specific target shape 在后端统一校验。Profile 保存会在规范化前及三方合并后复核，旧 Store、SessionSummary cache 和重连恢复均 fail-closed 丢弃非法/重复项；满列表只回收最旧 disabled 历史，不覆盖 enabled 定义。创建期间 Profile 被删除或改为非 SSH/Tmux 会使 Store 事务失败并触发现有 runtime 补偿清理。前端保持单层紧凑表单，使用同一 host 长度、整数端口和数量边界。

主要缺口：

- Jump Host 后端连接链路已支持多跳，逐跳 host key 验证和有 deadline 的 direct-tcpip 串接可用；会话设置可增删多跳 Jump Host，并可为每跳保存独立 password/passphrase secretRef、指定 identityRef、切换继承或自定义 host-key mode/alias/trust scope/rotation/IP 检查；目标 host-key 预扫描可经多跳链路执行；连接失败和扫描时可返回首个需要确认的 Jump Host host key，并通过同一确认弹窗逐跳信任后重连；目标会话临时输入的凭据不会覆盖跳板独立 secretRef。
- GSSAPI 已接入受限的真实客户端路径：当认证顺序包含 `gssapi-with-mic` 且其余方式仅为 Profile Vault/System File public-key、无需精确指纹筛选的本机 ssh-agent fallback、password、keyboard-interactive 或 none 时，Linux 系统 libssh backend 会按 Profile/last-success 顺序认证，并执行 host-key Store/TOFU/一次性确认、服务端方法声明检查、`ssh_userauth_gssapi`、PTY/shell、动态 resize、交互读写、exec/Tmux/Sysmon、三类 tunnel 和 keepalive/exec/SFTP 健康探针；有效票据优先于后续 fallback，GSSAPI 被拒绝/未声明时可回退公钥、password 或 keyboard-interactive，成功方式仍按既有策略记录。非 `IdentitiesOnly`、没有显式 Agent identity 引用的 `BeforeProfileKeys`/`AfterProfileKeys` 策略会把指定 agent socket 传给 libssh，并在 public-key 阶段按配置顺序调用 agent；显式 Agent identity/fingerprint 仍由 russh 精确过滤，因为 libssh 的 `userauth_agent` 不支持按单 key 筛选。Agent forwarding 也已接入 libssh terminal setup：先发送 `auth-agent-req`，再以最多 16 个活动 bridge、32 个底层待接收 channel 和固定 64 KiB 缓冲将服务端 agent channel 双向转发到显式 socket 或 `SSH_AUTH_SOCK`；socket read、write 和 libssh flush 均有界，runtime 关闭会等待 forwarder 与 channel 收敛。HTTP CONNECT/SOCKS5 Proxy 复用同一共享握手、凭据和协议诊断，在一个总连接预算内把预连接 socket 交给 libssh；项目内置 binding 会持有该 fd，并在 disconnect/session drop 时按 libssh 0.10 的调用方责任释放。Jump Host 复用已经逐跳完成 Host Key 与身份认证的 russh 多跳链，在最后一个跳板打开目标 `direct-tcpip` channel，再通过只绑定 loopback、精确校验 accept peer 的受控 TCP bridge 交给 libssh；runtime 关闭会先停止 bridge，再断开 libssh 和逐跳 russh session。russh 测试服务端到 libssh 客户端回归已覆盖 password/keyboard-interactive 回退及 stdout/stderr/exit-status/EOF；隔离 OpenSSH runtime 还覆盖错误公钥在前、加密正确公钥在后、错误 passphrase 失败且不安装 runtime、临时 passphrase 解锁、成功方式持久化，以及 GSSAPI 未声明后经真实隔离 ssh-agent 完成 public-key fallback 并确认实际使用 libssh runtime；同一真实环境还从远端 shell 执行 `ssh-add -L`，确认 forwarding 返回本机 agent identity 并有界清理，并分别经真实 loopback HTTP CONNECT/SOCKS5 完成 libssh 连接、拒绝诊断和实际代理计数。三台隔离 OpenSSH 组成的两跳链验证了三个端点的独立 identity/Host Key、目标 libssh runtime、PTY marker 命令和 bridge/逐跳 session 的有界清理。隔离 Profile Vault loader 回归覆盖 secretRef 路由、加密 key、错误口令、缺失 Secret/引用和不泄露 key 正文的错误诊断。当前 `russh` 0.62.4 仍没有 GSSAPI exchange。SFTP 文件浏览/属性/变更、upload/download/remote-copy、SCP upload/download/remote-copy 和三类 tunnel 均已迁入统一 backend 边界；隔离 OpenSSH 的真实 libssh 回归覆盖非空文件读写、断点文件关闭、目录列表、属性、chmod、rename、递归删除、三类 SFTP 传输、SCP 完成 marker 和延迟非零状态，以及 local/dynamic/remote reverse tunnel 的目标拒绝、后续恢复、双向数据、metrics、停止和 remote 监听撤销。OpenSSH native mux 因没有会话后的 window-change 请求，仍不作为完整交互后端。Linux build check 会拒绝未引用 `ssh_userauth_gssapi` 或所链接 libssh 未声明 GSSAPI runtime 依赖的构建，本机已对系统 libssh 0.10.6 + MIT Kerberos 链接通过；非 Linux 使用不带 GSSAPI 的 vendored libssh 仅保证构建。Ubuntu 24.04 OpenSSH 9.6、Debian bookworm OpenSSH 9.2、Debian trixie OpenSSH 10.0，以及 Apache MINA SSHD 2.19.0 的真实 MIT Kerberos/KDC/ticket cache 矩阵，加上 Ubuntu 24.04 OpenSSH + Samba 4.19.5 AD-compatible KDC，均已覆盖认证成功、GSSAPI 优先、严格 Host Key 拒绝、无票据、无票据 password 回退、服务端禁用 GSSAPI、禁用时 password 回退、SFTP subsystem 拒绝和 SFTP 目录操作拒绝九种场景；全部成功路径同时覆盖 TOFU、交互 shell I/O、keepalive/exec/SFTP health、成功方式记录和有界清理；四个 OpenSSH 服务端另验证真实 PTY resize，其中 SFTP 探针真实初始化 subsystem、规范化当前目录并读取目录项；两种 SFTP 故障均保持 transport/exec 正常并准确降级。Apache MINA 成功路径验证交互 shell I/O，但因 process shell 不提供真实本地 PTY，不断言 `stty` resize 尺寸；Samba provision 仅增加 SYSVOL ACL/xattr 所需的 `SYS_ADMIN` capability，并在启动 Samba/OpenSSH 前从 runtime bounding set 删除。Microsoft Active Directory 仍未实证，因此 GSSAPI 不能标记为完整实现。
- Runtime summary 已记录 `lastDisconnect`/`lastDisconnectReason`，SQLite mirror 同步保存；资源树会话和 pane 连接按钮现在通过不占布局的 tooltip 与 `aria-description` 显示六种中文状态、最近断开时间和原因，保持稳定的“连接/断开”可访问动作名称。原因会折叠多行空白并限制为 256 个 Unicode 字符，无效时间不显示；Chrome 工作区基线注入 SSH keepalive 断线记录，锁定资源树、连接按钮和精确动作定位，并覆盖浏览器 fallback 凭据保存失败时显示实际错误原因。同一次 outage 的 SSH/TCP/Telnet/Serial 重连失败、禁用重连和幂等手动关闭只更新原因并保留首次 `lastDisconnect`；恢复 Connected 后的下一次 transport loss 才记录新时间，共享 Store 单元回归和真实 TCP 断线/换端点/再次断线/停用链路已锁定。浏览器 fallback 复用同一状态转换规则，首次进入 `Connecting` 不会制造伪断线，首次成功连接不会留下不存在的断线时间，连接失败会保留具体原因。SSH/Tmux 的重连延迟/协议 KeepAlive、TCP/Telnet 的 OS keepalive/重连延迟，以及 Serial 的重连延迟/接收空闲阈值均可按 Profile 配置，断线后会进入 `Reconnecting` 并后台重试；更广故障矩阵和更深连接健康诊断还不完整。
- Rust Store 在持久化前同样折叠断线原因空白并限制为 256 个 Unicode 字符，空原因回退到状态默认诊断；加载旧 SQLite/兼容快照时也会归一化历史字段。浏览器 fallback 现在也在更新可缓存 runtime 之前执行同一字符边界和默认原因，不再只在 tooltip 渲染时截断；结构合法的旧 SessionSummary cache 在独立窗口消费前同样规范该字段，不会因单个超长历史原因丢弃其余可用会话。前后端 formatter 单次流式扫描且只保留有界输出，不会为被篡改的超大字段构造等长副本或等长字符数组；单元回归覆盖多字节字符、精确边界、第 257 字符、超大前导空白、空原因和 legacy 加载。
- 独立串口分析器复用共享 runtime health formatter：标题栏显示固定宽度中文状态并提供完整 tooltip/`aria-description`，状态条只在存在有效诊断时显示一次断线详情；非法时间、多行空白和超过 256 个 Unicode 字符的原因均按主工作区同一边界处理。Chrome 工作区矩阵锁定文本、可访问描述、固定宽度和真实截图，不再出现内部英文状态、`Invalid Date` 或 `上次断开 --` 占位。
- 独立终端窗口的单行 footer 和状态点复用同一 runtime health 描述，不再显示 `connected`/`missing` 内部值；`Connecting`/`Reconnecting` 与主窗口一致显示可取消的断开动作，`Disconnected`/`Blocked`/`Error` 才提供连接动作。Chrome popup 回归锁定有界中文诊断、唯一动作、发往主窗口的精确 `disconnect` 命令和无重叠截图。
- 桌面连接请求按 session 单飞：重复点击不会排队第二次 save/open，手动断开会失效当前代际、关闭同 session 凭据弹窗并淘汰迟到的 save/open/log 结果；资源树和 view 右键菜单在 pending 状态禁用重新连接但保留断开。显式重新连接严格等待 `close_session` 成功后才执行 save/open，关闭失败不会继续，并把权威 close summary 作为新 `Connecting` 代际的基线，因此握手期间不会回滚刚记录的断线时间/原因。后端生命周期锁内还会拒绝 Store 处于 `Connecting`/`Connected`/`Reconnecting` 或任一 transport registry 仍持有 runtime 的重复 open，避免非 UI 调用替换并泄漏旧 transport。Rust 回归锁定原 runtime ID/事件不变和残留 registry 拒绝，Chrome 隔离页面锁定取消、迟到响应、双击、菜单能力、断线诊断与 `close -> save -> open` 顺序。
- Serial 的断线重开、最新 Profile 重载和过期尝试拒绝已接入；精确 Hex/ASCII、时间戳、方向过滤、增量查询、清空与 JSONL 导出已接入会话侧栏。工具菜单和侧栏可打开真正独立的 Tauri WebviewWindow 串口分析器，浏览器预览降级为同源 popup；窗口自行轮询捕获/连接状态，定时与手动会话状态刷新共用单请求门控，慢响应不会与后续周期或刷新按钮并发。分析器支持后端读取分片、可跨读取边界但不跨 RX/TX 的 Hex 分隔符、1..4096 字节定长、1..60000 ms 空闲间隔、RFC 1055 SLIP、COBS 和 Modbus RTU 七种 framing。SLIP 使用浏览器兼容的成熟 `slip` 解码器，COBS 使用零依赖且面向浏览器/Web Serial 的 `@serialpilot/cobs` 解码器；两者都跨捕获分片保留状态。Modbus 使用 `@serialpilot/modbus-rtu` 解码站号/功能码/PDU、校验 CRC-16、识别异常响应，并可按 Profile 波特率自动计算 3.5 字符静默阈值或手动设置；历史聚帧只使用同方向捕获读取的时间戳，不声称拥有逐字节时间，OS 单次读取内合并的多帧会保留为 CRC 错误证据。所有模式逐帧保留解码载荷、原始线上字节和精确源 ID；SLIP 无效转义会标错但不丢失解码结果，COBS/Modbus 错误帧保留完整线上证据而不伪造部分载荷；详情可在解码/线上证据间切换，筛选同时搜索两者。分析器默认继续使用 512 帧/1 MiB 实时内存环；Profile 显式启用 Raw 日志后，可切到不自动轮询的日志源，从最近 5,000 个持久事件中重建至多 4,096 帧/8 MiB。每个 `bytesRef` 都重新校验相对路径、范围和 SHA-256，窗口外与损坏引用分别计数，单帧显示仍限制 64 KiB 并保留原长度；实时清空不会删除日志，历史导出会重新确认同一事件窗口后原子写入 JSONL + sidecar。分析器还提供方向/Hex/ASCII/书签筛选、跟随、键盘行导航、100/250/500 分页、4,096 派生帧显示环、完整 Hex dump、跨解析模式书签、精确源帧导出，以及端口/线路参数/重连中/最近断开原因可视化。
- SFTP 文件管理已是 local/remote 双栏并从跨平台 `~` home alias 打开本地面板，支持 Ctrl/Command 切换、Shift 连选、全选、复制选中路径、一次预检的批量删除、同窗格 `Move To...`，以及单项 rename/chmod/属性查看；多文件和完整目录可通过按钮或面板拖拽上传/下载，远端目录由 SFTP 递归枚举并保留空目录、跳过 symlink。本地/远端 create/delete/rename/move/chmod 会在打开 SFTP channel 或触碰文件系统前拒绝根目录、当前/父目录别名、显式 `.`/`..` 分量和中间 symlink；批量删除和移动会拒绝重复路径、目录与子项同选及目录自包含目标，执行竞态错误会返回已完成数量；远端批量源与拖放目标使用同一分量边界。create/chmod 也拒绝最终 symlink，delete/rename/move 则可操作最终 symlink 本身但不会跟随它；真实 OpenSSH/SFTP 回归确认经链接目录的 create/chmod/rename/delete 均被拒绝且目标内容和权限不变。两侧目录读取按 pane 记录请求代际，较慢的旧响应不能覆盖较新的路径或 loading 状态；切换 SSH/Tmux Profile 会立即失效上一会话的远端读取并从 `.` 重新加载，避免迟到列表显示到新会话。属性检查器在关闭、重开或换会话时同样失效旧文件响应。内部批次和 Tauri 原生外部拖放共享 `fail/overwrite/skip/rename` 冲突策略，在目标修改前完成类型/路径冲突和超限检查，并跟踪整批 task 终态后刷新目标；local/SFTP/SCP 分块传输已有进度、速度、取消、失败重试、profile 级协议授权/B/s 限速/默认本地目录和 `.portmate-part` 临时文件与续传（local copy、SFTP upload/download/remote copy、SCP upload 为偏移续传，SCP download 从 0 安全重试），限速等待会让出 async runtime 并每 100 ms 检查取消；远端命令型复制保持单个 channel wait，每 100 ms 检查取消，仅由有效且单调、与总大小一致的 size/resume/progress/done marker 刷新 60 秒空闲 deadline，同时保留 300 秒总 deadline、4 MiB/64 KiB stdout/stderr 上限和有界 channel 收尾；传输任务已改为后台执行并按 session 串行排队，弹窗已有当前会话全量队列视图和批量取消/重试入口。失败任务会显示远端错误、部分进度和失败时间，可复制完整诊断；受限长度的失败原因同时写入 session event，避免只有 `Failed` 状态而没有上下文。外部二十服务端矩阵现包含十个 OpenSSH/Dropbear 服务端的交错断线、AsyncSSH 2.24.0 与 Paramiko 5.0.0 Debian Bookworm 的 PTY/三层健康/SFTP/SCP 正常路径，以及独立 ProFTPD `mod_sftp`、SFTPGo 2.6.6/2.7.5、rclone 1.74.4、Erlang/OTP 25.2.3/27.3.4.1 `ssh_sftpd`、Go `github.com/pkg/sftp` 1.13.6 Debian trixie 和 Apache MINA SSHD 2.19.0 八个 SFTP-only 组合的上传/下载/远端复制；Apache MINA 额外验证协商 SFTP v3 后返回的 v4+ `FileAlreadyExists`/`DirNotEmpty` 状态可在 5 秒内准确收敛，本地 `russh-sftp` 补丁可解析标准 9-31 状态码并保留未知状态包，递归建目录会先 `LSTAT` 以避免对既有目录发出重复 `MKDIR`；独立故障服务器额外验证缺失远端源文件和只读目录的 SFTP `NoSuchFile`/`PermissionDenied` 语义，以及原有 subsystem/command 拒绝。
- 文件管理器的本地/远端目录各自保留有界后退/前进历史，只有成功的目录读取才会写入；刷新、文件 mutation、失败和被代际淘汰的迟到读取不改变下一次导航目标。
- 文件管理器新增本地/远端空文件创建；创建使用排他打开，不会截断已有文件，也不会穿过最终或父级符号链接，并沿用现有根目录、`.`/`..` 路径分量限制。
- ZModem 当前限制单文件小于 4 GiB；OpenSSH PTY 上的 lrzsz X/Y/Z 双向传输已有实测，静默 modem 等待可在取消后立即发送 CAN 并快速退出，transport 进入重连态会立即失败旧 worker；十个 SSH 服务端（OpenSSH Alpine 3.19/3.20/3.21、Debian Bookworm、Debian trixie、Ubuntu 24.04、Fedora 44、GESFTPServer Ubuntu、Dropbear Alpine 3.20/3.21）已在独立 SSH runtime 上交错验证 X/Y/ZModem 活动断线，ProFTPD `mod_sftp`、SFTPGo 2.6.6/2.7.5、rclone 1.74.4、Erlang/OTP 25.2.3/27.3.4.1 `ssh_sftpd`、Go `github.com/pkg/sftp` 1.13.6 和 Apache MINA SSHD 作为 SFTP-only 服务端另行验证文件传输；物理串口、批量和不同工具实现的兼容矩阵仍待补。

### SSH 身份与 Host Key 隔离

状态：核心设计已落地，交互还需强化。

已实现：

- `SessionProfile` 持有 `hostKeyPolicy`、`trustedHostKeys[]`、`identityPolicy`、`identityRefs[]`、`agentPolicy`。
- 默认不写系统 `~/.ssh/known_hosts`。
- 支持 `hostKeyAlias`/profile alias，同 IP 同端口不同 profile 不冲突。
- 支持同一 alias 下多 host key algorithm。
- Host key mismatch 默认阻断。
- TOFU 模式会保存到 profile 级 trust。
- client identity 默认按 `IdentitiesOnly` 思路，只尝试 profile/keyring 指定 key，不遍历系统 agent。
- 当 profile 配置 `Profile + Agent` 或显式 agent identity 时，后端会读取本机 ssh-agent 并按 `offerMode` 顺序尝试认证；未关闭 `IdentitiesOnly` 时不会无界遍历全部 agent key。
- SSH/Tmux 只按当前 Profile 的 `authOrder` 尝试认证；启用 `recordSuccess` 时，成功方式会随连接状态一起持久化到 `lastSuccessful`，但仅在它仍属于当前顺序时才于下次初连/重连前置。用户移除该方式或关闭记录后，前端草稿、旧 Store 加载和共享 Store 写入都会清除/拒绝越界提示，不会让历史成功方式绕过当前策略。紧凑下拉菜单覆盖 public-key、keyboard-interactive、password 的全部 15 种有序非空子集；含外部/旧认证方式的现有值会作为“当前配置”显示，不再出现空白控件或静默改写；相邻的“记住成功方式”开关可直接控制 `recordSuccess` 并在关闭时清空草稿提示。Chrome 工作区矩阵会实际选择非默认 `keyboard-interactive > public-key`、关闭该开关并复核菜单唯一性。
- 私钥、密码、passphrase、MCP IPC token 进入持久 OS keyring，SQLite 只保存引用。Linux 只使用 Secret Service，不再把重启即丢的 kernel keyutils 当作持久 provider；初始化瞬时失败不会永久缓存，后续操作会重试，并让自动存储按既定规则进入已解锁 Stronghold fallback 或返回明确错误。原生 provider probe 会以三个独立进程验证 missing/write、跨进程 read/update/read-back 和 read/delete/missing/幂等 delete，并使用 1,200 字节 Secret；Linux 已在隔离 HOME/XDG 和真实 `gnome-keyring-daemon`/D-Bus session 下通过，还会把无 provider 映射为 `PlatformFailure`、把锁定 collection 且无图形解锁通道映射为 `NoStorageAccess`。Windows Credential Manager 与 macOS Keychain 已接入 Native CI，但仍需对应 runner 的成功记录和锁定/拒绝证据。
- OpenSSH `known_hosts` 导入/导出已接入 Tauri command 和密钥管理器弹窗。
- 密钥管理器可以查看 PortMate host key trust store、按 scope/profile 过滤分组、导入/导出 known_hosts、删除 host key、批量删除/复制 host key 到选中 profile、编辑 host key 的 alias/host/port/scope/profile/label；Client Key 区域可搜索并按 profile/source 筛选分组全部 SSH/Tmux profile identity，批量复制到目标 profile、在各自 profile 中置顶或移除引用，被 Jump Host 使用的引用会标记并阻止移除；可从本地文件或粘贴内容导入 OpenSSH 私钥到 profile-vault，并单条或批量添加当前 ssh-agent identity；紧凑 identity inspector 可编辑 label/source/path/fingerprint，展示不可变 ID、Jump Host 和共享 secret 影响，并安全轮换 Vault 私钥或区分“只移除引用/同时清理未共享 secret”。
- 首次连接/host key 变更失败后会弹出专门确认窗口，展示 SHA-256 指纹/已保存指纹，并支持仅本次、加入 Profile、加入 Project、替换 Profile、拒绝和确认后重连。
- `AskEveryTime` 会在每次连接时要求显式确认，不再因永久 trust store 中已有 key 而直接放行；`TrustOnce` 只对精确匹配的 profile/alias/host/port/algorithm/fingerprint 生效。
- 多跳连接的 `TrustOnce` 会保留到整条 Jump Host 链成功建立后再消费，后续跳点确认失败不会导致前一跳重复确认。
- OpenSSH `known_hosts` 导入/导出会保留非 22 端口的 `[host]:port` 形式，并保持 `checkIp` 可匹配规范化 host。
- Host Key 的成功观察会持久化 `lastSeen`，并在同一 Store 提交中同步全局记录和 Profile 内镜像；Manager 的筛选、分组、批量复制/删除和字段检查器均使用该一致视图。

主要缺口：

- 目标 host key 扫描已可经多跳 Jump Host 链路执行；多跳连接链路、每跳独立凭据/host-key 策略编辑和逐跳 host key 确认弹窗可用。
- Bitvise 风格 Host Key Manager / Client Key Manager 已有 Host Key 分组过滤/批量删除/批量复制、host key 字段编辑、Client Key 搜索/分组/批量复制/置顶/安全移除、私钥文件导入、ssh-agent 批量添加、identity 字段检查器和 Vault 私钥轮换。Host Key Inspector 会冻结打开时的 `expectedKey`，后端按当前/预期/提交值三方合并 `profileId/alias/host/port/scope/label`，非重叠字段自动合并，同字段冲突只返回不含内容的 `Host Key` 字段路径；算法、指纹、公钥和观察时间不会被旧表单覆盖，全局 Store 与 Profile 内同 ID 副本在同一事务更新。Identity Inspector 同样冻结 `expectedIdentity`，未修改的 Secret 引用会保留并发轮换结果，同字段冲突标记为 `Client Identity` 而不会误报成 Profile。identity 更新/轮换/删除由后端按 immutable ID 原子持久化；共享 secret 不会被原地覆盖或误删，孤儿清理失败只返回 warning，不会留下悬空 Profile 引用。
- IOTA Stronghold portable vault 已接入：使用独立 Argon2id salt 派生 snapshot key，支持创建/解锁/锁定、`stronghold:` secretRef 路由和显式存储选择；自动模式优先 OS keyring，仅在 native 写入失败且 Stronghold 已解锁时 fallback。SQLite 仍只保存引用。已解锁 vault 可在验证当前密码后更换主密码；新密码至少 8 个字符且必须不同，snapshot 使用新派生 key 通过 Stronghold 临时文件提交，提交成功后才替换内存 provider，原 `stronghold:` 引用和 secret 内容保持不变，旧密码无法再解锁。解锁、保存与换密在进程内共用串行化边界；open/save/rekey 另使用跨进程 OS 文件锁并在写前比较已加载 snapshot 的 SHA-256 版本，stale 实例会被拒绝并要求重新解锁，不能用旧 provider 覆盖换密结果。Linux 已用独立进程验证 1,200 字节 Secret 的 write/read/update/delete、错误主密码、损坏 snapshot、缺失 salt、提交失败内存回滚和真实 stale snapshot CAS；Unix vault 目录会收敛到 `0700`，snapshot/salt/lock 收敛到 `0600`，并拒绝父目录或文件 symlink、非普通文件及多硬链接。该矩阵已接入 Windows/macOS Native CI，但仍需对应 runner 的成功记录。SSH/Tmux 的目标密码、passphrase、Profile Vault 私钥和逐 Jump Host 凭据可按全部或单个 profile 在 native keyring 与 Stronghold 间双向迁移；同源引用只复制一次，Stronghold 批量写只提交一个 snapshot，native 写入会精确读回，预检 token 和 SessionStore revision 在任何目标写入前复核。Profile 采用 copy-on-write 一次提交，旧 secret 仅在全局无引用且没有建连中 reader 时清理。迁移先以 `synchronous=FULL` 写入不含 secret 正文/正文 hash 的 SQLite journal，目标写入只有在 Prepared 精确读回后才开始；Profile KV、mirror、revision 与 `profiles-committed` 是同一事务中的唯一 commit point。重启后按结构化 credential slot 投影分类：全 OLD 只回收未引用目标，全 NEW 在逐项验证目标及源/目标内容一致后继续源清理，混合/缺失/第三种投影或 provider 不可判定时保留两侧并冻结自动恢复。Key Manager 会在打开、vault 锁/解锁和迁移结束后刷新 pending 状态，提供显式“核对并恢复”，pending/corrupt journal 会阻止新的迁移及相关 Profile/secret/identity mutation。恢复面板还能原子导出 JSON + SHA-256 诊断，包含 before/current/after slot、provider 存在性、引用计数和内容一致性布尔值；secret 正文/正文 hash 以及无法验证的原始 corrupt payload 均不会进入文件。MCP token 始终排除在该范围外。

### 数据、日志与自动化

状态：结构化数据可用，日志体系还没达到最终目标。

- Portable vault 的状态读取、恢复资格判断与底层加密操作已集中在 `src-tauri/src/portable_vault.rs`，vault command 只负责授权边界和请求编排。

已实现：

- SSH window-change、Shell PTY resize、Telnet NAWS 和 terminal profile 尺寸提交已集中在 `src-tauri/src/session_terminal.rs`，根模块只保留跨模块共享状态与测试入口。

- SQLite `portmate-store.sqlite3` 为主存储，并保留 JSON 兼容导出；兼容文件使用同目录随机临时文件同步后原子替换，Unix 权限固定为 `0600`，预置的固定临时路径或目标 symlink 不能把快照写入其他文件。
- SessionStore 保存使用跨进程 sidecar 文件锁和绑定 `storeRevision + kv` 内容的 CAS；stale 实例、旧 writer 直接修改 kv、提交后无法验证以及损坏 SQLite 均拒绝继续覆盖。首次创建/旧 JSON 迁移在同一锁内完成，单纯启动第二实例不会旋转 revision。
- SQLite v4 mirror tables：profiles、runtimes、events、transfers、trusted_host_keys、mcp_grants、mcp_audit、timeline_marks、带结构化 `details_json` 的 sysmon_snapshots，以及独立的 profile credential migration journal；v3 Sysmon 表会原位增加默认空 details 列。
- SQLite mirror 在同一事务内更新完整 kv 快照；profiles/runtimes/transfers/keys/grants 等小型可变表重建，events/audit/timeline/sysmon 按主键增量插入并清理已裁剪项，避免日志增长后每次保存重复重写全部大表。
- 会话事件、屏幕文本、传输任务、host keys、MCP grants/audit、timeline、sysmon 都进入统一 store。events 每会话保留目标 5,000 条并使用 512 条实时批量裁剪余量；审计按 session/global scope 保留目标 5,000 条、timeline 每会话 2,000 条、Sysmon 每会话 1,024 条，并使用 128 条余量；终态 transfer 每会话精确保留 1,000 条，queued/running 永不淘汰。桌面加载旧 Profile 时会把被裁掉首尾空白的 ID 同步迁移到 runtime、event/pane、transfer、audit、timeline、Sysmon、Host Key、MCP grant 和 OneKey 引用，避免 Profile 可见但关联历史静默孤立；桌面和独立 MCP 加载旧快照时还会把每个 event/history scope 收敛到精确上限并重建 event count cache，防止未继续产生日志的旧会话绕过边界，也避免 KV JSON、内存和 mirror 同步成本无限增长。
- 活动 transfer 队列按 Store 中 queued/running 状态同时限制为每会话 5,000 项和全应用 5,000 项；独立的 5,000 permit runner semaphore 持有到异步任务真正退出，取消后不能靠提前变成终态绕过容量并堆积后台 future。等待 per-session lane 的 queued worker 使用通知立即取消并释放 permit。递归文件批次和外部拖放在创建目标目录前预检 Store/runner 容量，单任务事务仍在写入前复核权威活动计数。
- terminal stream 可按 profile 设置追加写入 raw/text/jsonl 分片；入站 Raw 保存 SSH channel、PTY、Raw TCP/Telnet socket 和 Serial 解码前精确字节，出站 Raw 保存成功用户 text/bytes 经协议编码后的 wire、Telnet 协商回复和 modem 帧。每会话 lane 保证出站 transport/write/event 顺序；每个 `bytesRef` 精确绑定对应字节，但双向并发时不承诺共享分片的跨方向因果排序。写成功后的 SQLite/Raw/Text/JSONL 降级会通过事件 `loggingError` 报告，不会伪装成发送失败；二进制结构化事件只保存长度，不复制可逆 Hex。最终分片路径上的追加、读取和删除互斥；新 `bytesRef` 带 segment SHA-256，可拒绝删除重建/修改后的错误内容，同时兼容旧 path/offset/length 引用。新 profile 默认关闭日志和 Raw，UI 明示 Raw 不脱敏。
- Text 分片的每个逻辑行带固定三位 UTC 毫秒时间、方向/stream、转义后的 session/pane 和 command ID（无关联为 `-`）；多行事件逐行重复元数据。Raw 保持纯 wire bytes，并由 JSONL 事件与 `bytesRef` 承载相同来源信息。Raw/Text/JSONL 会使用事件开始时的同一 profile 快照，配置并发更新不会把一个事件拆到不同路径；Text 失败会进入随后 JSONL、内存和 SQLite 的 `loggingError`。
- 显式 `run_command` 在 transport write 前生成 UUID `commandId`，命令出站事件带 `commandState=started`，后续入站事件继承同一 ID；下一条显式命令会替换，普通/OneKey/modem 输入、open/close、断线和重连会清理。该模型不猜测 prompt 或命令结束，快速回显不会先于关联激活，写入失败也不残留；annotations 会保留到 SQLite、JSONL、MCP 和 session bundle。执行型 Quick Command 使用该路径，已加载日志可按完整 ID 搜索并显示短 ID。
- direct、连接生命周期、trigger、重连、transfer、tunnel 等 system/control 事件通过 Core 的单槽 wake + 4,096 条有界 outbox 统一写入脱敏 Text/JSONL 并实时 emit；system sink 不创建或追加 Raw，正常运行中每个入队事件只发布一次，退出时会停用共享 notifier、drain 并 join worker。积压超限或 worker 断开会在 store event 写入 `loggingError`，不会无限增长或静默丢弃后续诊断。事件后补 annotations 会按内容变化更新 SQLite `events` mirror，不退化为重复插入全部历史。
- `工具 -> 日志管理` 可安全枚举 raw/txt/jsonl 分片，按路径和格式筛选、查看受限尾部预览（raw/非 UTF-8 使用 Hex）并批量清理；扫描、预览和删除均有数量/大小上限，symlink、路径穿越和非分片扩展不会进入操作范围。
- 日志分片列表、预览和内容搜索按请求代际隔离；快速选择 A/B 后 A 的慢响应不能覆盖 B，刷新、删除、查询变更或关闭弹窗会失效不再对应当前界面的结果。
- 日志管理可全文搜索磁盘 Text/JSONL 分片，包括已从有界 store events 裁剪的历史；支持全部或选中路径，结果带分片、行号、字节偏移和受限上下文，并明确报告命中/单文件/总扫描上限与 warning。Raw 保持 Hex 预览，不伪装成文本搜索。
- 日志管理可把最多 1,000 个、合计不超过 512 MiB 的选中 raw/txt/jsonl 分片流式归档为原子落盘的 `.tar.gz`，包内 manifest 记录逐文件 SHA-256，并生成 `.sha256` sidecar；源分片保留不删除，路径穿越、symlink、非法扩展和归档过程中截断的文件会被拒绝。
- 每个 profile 可配置 0..=3650 天自动保留期；旧配置默认关闭。应用启动时后台检查，持续写入时最多每小时复查一次，只按 profile 模板匹配并在删除前二次核对 mtime，随后清理空目录；内存检查 registry 按 Store/Profile 只保留当前 `(retentionDays, checkedAt)`，设置变更覆盖、过期一小时回收，禁用/保存/删除 Profile 会清理对应项，不再按历史天数永久累积。启用保留期的自定义模板必须含 `{session}` 或 `{profile}`，避免误删共享路径。
- 日志管理可把选中会话导出为原子落盘的 `.tar.gz`、`.sha256` 和 Ed25519 `.sig.json` sidecar；包内含 bundle JSON、events JSONL、平台/store 诊断和逐文件 SHA-256 manifest。签名覆盖最终归档名、SHA-256、大小和创建时间组成的版本化 NUL 分隔 payload，sidecar 携带独立验证所需的公钥和精确 payload；长期 seed 只进入系统 keyring 或已解锁 Stronghold，内部固定引用不会被通用 secret/Profile/迁移接口覆盖。默认脱敏会移除 Profile/Jump/Proxy credential ref、identity 路径、已配置的 runtime/logging/transfer/Shell 工作路径、Shell 参数、自动化发送/命令/链接载荷、传输任务路径、Sysmon 进程/文件系统/挂载点/接口标签、diagnostics 日志分片清单和 event `bytesRef`/`logShards`，并脱敏 event/annotation/timeline/audit/transfer message/断线原因/`summary.lastLine` 文本，同时保留 Sysmon 数值指标、协议、状态、endpoint 和公钥指纹诊断。脱敏开启时强制排除 raw 并禁止附件；只有显式关闭脱敏后，才可附加当前明确选中的最多 32 个、单个 16 MiB、合计 32 MiB 的受限日志分片，重复文件名会安全编号，读取期间的 symlink、截断和替换会失败。只有另行启用 raw 后才按受限 `bytesRef` 读取片段。
- 触发器支持多个 contains/regex 规则和每条规则的有序多动作编辑；动作包括高亮、通知、时间线标记、本地命令、发送文本、自定义链接和 bell/chime/alert 声音。每个 Profile 最多 64 条规则、每条 16 个动作，ID/名称/matcher/正则编译体积/动作载荷/颜色/声音均由后端验证，紧凑编辑器同步限制输入和原位禁用添加按钮；保存拒绝重复、超限和非法定义，旧 Store 加载与运行时求值 fail-closed，不执行被截断的命令。运行时视觉/声音效果通过 Tauri event 立即送达桌面，所有副作用先核对产生输入的 transport `runtime_id`，旧 reader 的迟到输出只保留日志而不影响替换连接。自定义链接的 `{text}` 展开以流式方式限制为 8,192 个 Unicode 字符，不先构造可能被重复占位符放大的完整 URL，截断会明确写入会话诊断。send-text 按输入事件组成最多 32 条的顺序批次，全应用最多 8 个并发批次，超限/饱和写入诊断而不建立无界 task 队列；批次每次写入仍核对来源代际。每次输入最多派发 8 个本地命令，全应用最多 4 路并发、30 秒总时限和 stdout/stderr 各 64 KiB；批次溢出与饱和跳过均聚合成单条诊断，不进入无界队列或逐条持久化，超时会显式终止并回收直接子进程，Unix 还以独立进程组清理仍持有管道的后代。
- secret redaction 有核心测试。

主要缺口：

- 传输任务对 local/SFTP/SCP copy loop、远端命令型复制目标大小轮询和 X/Y/ZModem block loop 已有实时进度/速度与取消。

### MCP Surface

状态：stdio bridge 可用，基础 HTTP JSON-RPC 模式可用。

已实现：

- `portmate-mcp` stdio bridge，JSON-RPC lifecycle/tools/resources/prompts。
- 官方 TypeScript SDK 1.10.0/1.20.0/1.29.0/1.30.0 矩阵使用隔离 npm lock/node_modules 启动真实 bridge 子进程，分别覆盖 `2024-11-05` 旧生命周期、`2025-06-18` 正常协商，以及 1.29.0/1.30.0 从 `2025-11-25` 请求回退到服务端 `2025-06-18`；每版都完成 initialize/initialized、ping、tools/resources/templates/prompts/resource read 共 8 条消息，并验证客户端关闭后 bridge 自行退出。
- stdio 每条 newline-delimited JSON payload 上限为 1 MiB（不含 `LF/CRLF`）；超限行仅保留 `limit + 2` 字节并有界丢弃到换行，返回 JSON-RPC parse error 后可继续处理下一条消息，不会无界分配或协议失步。
- stdio/HTTP 共用的 JSON-RPC envelope 会保留显式 null ID，拒绝对象/数组/布尔 ID 和非结构化 `params`；batch 在任何 tool dispatch 前限制为 128 项，避免小请求放大为无界调用与响应。
- stdio/HTTP JSON-RPC 响应与 SSE JSON 数据共用 64 MiB 有界 writer，写入将在追加越界前停止。单响应超限会返回保留原 ID 的 `-32603`；batch 或 SSE 状态超限会以不含原大 payload 的受限错误替换，避免日志、screen 或状态数据造成无界二次序列化和输出。
- MCP protocol version 使用 `2025-06-18`。
- Tools：`list_sessions`、`read_screen`、`tail_log`、`search_logs`、`send_text`、`send_key`、`run_command`、`open_session`、`close_session`、`start_transfer`、`create_tunnel`、`list_tmux_state`、`attach_tmux`、`export_session_bundle`。
- Resources：sessions、state、screen、log、timeline、sysmon、tmux、transfer。
- Prompts：diagnose、serial/SSH compare、repro report。
- grant Store 为空时默认只读；存在任意 grant 后，读工具、资源、提示和 SSE 状态按 client 的 `read-sessions`/`read-logs` 与允许会话显式过滤。写操作始终通过 MCP grant scope 控制；可选 `confirmWrites` 会在每次写入前请求一次性桌面批准，最多 32 个 pending、60 秒超时并 fail-closed，批准 ID 只可消费一次。
- live desktop Store 是读写授权的最终来源；bridge 会先按最新有效本地快照过滤只读 fallback，desktop IPC 再按 live grant 复核并拒绝清单外命令。通过 IPC token 认证的写尝试会按 client ID、真实 tool、session、scope 和 `invalid`/`denied`/`authorized`/`succeeded`/`failed` 结果持久化审计，授权记录在副作用发生前先提交；审计不复制原始参数、命令文本、密码、passphrase 或路径正文。MCP 出站事件的 actor 也使用真实 client ID，不再误记为 `desktop-user` 或额外生成 `send_text` 审计。显式 grant 按配置 scope 生效，`PORTMATE_MCP_TRUSTED=1` 仅额外允许空 grant Store 的本地开发 bootstrap。
- 桌面 MCP IPC 最多同时处理 64 个连接，饱和连接在不创建新任务的前提下以 100 ms 写入预算返回受限错误并关闭；请求体上限为 1 MiB，完整读取和响应写出各有 5 秒超时。超限、慢速未完成、JSON 无效和 token 无效的请求都在命令分发前拒绝，不进入审计，避免未认证本地进程无限占用任务或内存。
- `portmate-ipc.json` 通过同目录私有临时文件同步落盘后原子替换；Unix 最终权限强制为 `0600`，包括 keyring 不可用时含明文 token 的 fallback。替换不会跟随既有 symlink，失败会保留上一个完整 endpoint，并回收本次未发布的 keyring token。发布与退出通过跨进程文件锁串行化；新实例只在成功替换后回收同 Store、私有普通旧 endpoint 中规范 `keychain:ipc-<uuid>` token，正常退出只删除内容仍匹配本进程的 endpoint，并始终回收本进程跟踪的 keyring token，旧实例不会误删新实例的 endpoint。
- bridge 仅加载普通、≤64 KiB 且 Unix 无 group/world 权限的 endpoint 文件；`storePath` 必须匹配当前 Store，地址必须是 loopback `SocketAddr`，keyring 引用必须是 PortMate 生成的规范 `keychain:ipc-<uuid>` 且不能与 inline token 并存。bridge 到桌面的请求/响应分别限制为 1 MiB/64 MiB，并设置 3 秒连接、5 秒写入和 180 秒总响应 deadline；新增总预算容纳 60 秒审批且不侵占原动作执行预算，避免篡改 endpoint 触发任意远端连接、读取其他 keyring 记录或无界等待/分配。
- 长运行 stdio bridge 会在每个 JSON-RPC envelope 前重新加载最新有效 Store 快照和原子发布的 endpoint；桌面重启、IPC token/address 轮换无需重启 bridge，endpoint 删除会立即清空 live forwarding，Store 暂时不可读时则保留最后一次有效只读快照。endpoint 正常缺失不会反复输出错误日志。
- 桌面运行时通过本地 IPC 转发真实控制动作，IPC token 优先存 keyring。
- HTTP 模式通过 `--http` 或 `PORTMATE_MCP_HTTP=1` 启动，仅允许 loopback 绑定，校验 `Origin`，并要求 Bearer 或 `X-PortMate-MCP-Token`；HTTP token 优先来自 `PORTMATE_MCP_HTTP_TOKEN`，否则存入 OS keyring；支持 JSON-RPC POST、streamable-http JSON Accept 兼容、GET SSE 事件流和纯 SSE POST message 事件响应。POST 必须使用 `application/json`（允许 charset 参数），显式 `MCP-Protocol-Version` 必须匹配 `2025-06-18`，该版本头已加入 CORS preflight allow-list。
- MCP sidecar 的 HTTP/stdio transport 已从大型 `main.rs` 拆到独立 `http_server.rs`/`stdio_server.rs`。HTTP 边界统一拥有 loopback listener、64 连接容量、socket deadline、Origin/token 门控、JSON/SSE response negotiation 和长连接 keepalive；stdio 边界统一拥有 1 MiB 消息上限、超长行有界丢弃与下一消息恢复、parse error 和有界响应写出。tools/resources/prompts、Store/IPC 刷新与共享 JSON-RPC dispatch 继续留在协议业务层。36 个 MCP 单测、根 TypeScript HTTP/stdio smoke、TypeScript 1.10.0/1.20.0/1.29.0/1.30.0 和 Python 1.9.4/1.12.4/1.15.0/1.21.2/1.23.3/1.28.1/1.29.0/2.0.0 SDK 生命周期均在拆分后通过。
- HTTP 明确采用 MCP 允许的无状态 Streamable HTTP 模式，不签发 `Mcp-Session-Id`，因此没有伪造的 session/DELETE/replay 生命周期；官方 TypeScript SDK 四版本矩阵会启动真实 bridge，每版覆盖 initialize/initialized、GET SSE、ping、tools/resources/templates/prompts/resource read 共 9 个请求，并核对认证、双 Accept、1.10.0 无后期版本头的旧行为和 1.20.0/1.29.0/1.30.0 协商版本头。
- HTTP bridge 最多同时保留 64 个连接（含长连接 SSE）；完整请求有不可被 trickle byte 延长的 5 秒总 deadline，每次普通/SSE 写入有 5 秒 socket timeout，超额连接立即返回 `503`，普通 HTTP/1.1 响应显式关闭连接，避免未认证本地进程无限占用线程。请求头通过 `httparse` 严格解析并限制为 64 KiB/128 项；body 可使用 `Content-Length` 或唯一的 `Transfer-Encoding: chunked`，解码后仍限制为 1 MiB，chunk framing/trailer 另限制为 64 KiB/128 项并共享同一总 deadline。重复/冲突 framing、其他 transfer coding、畸形 chunk/trailer 和终止 body 后额外字节均在 JSON-RPC 分发前拒绝。重复 `Accept` 会按列表合并，Bearer scheme 不区分大小写，`q=0` 媒体类型不会被误选。
- MCP Bridge 弹窗已提供 HTTP endpoint、Origin、启动命令、tokenRef 展示，以及 keyring token 生成/轮换入口。
- MCP 授权变更、HTTP 配置/Token、审计刷新/导出按任务使用弹窗级请求门控；重复切换标签不会重叠慢配置读取，操作期间对应控件禁用，关闭弹窗会失效迟到响应。
- 密钥管理器的 Agent 枚举、Portable Vault 状态和凭据迁移恢复检查分别串行化；瞬时读取失败保留最后确认状态，解锁/锁定/轮换会先失效旧 Vault 读取，变更后的恢复检查显式替换旧检查。

主要缺口：

- HTTP MCP 已补 `Accept: application/json, text/event-stream` streamable-http JSON 兼容回归，GET `text/event-stream` 基础事件流、纯 SSE POST 的 `message` 事件响应，以及 Content-Type/协议版本/CORS preflight/严格 framing/重复头/quality value 回归。官方 TypeScript SDK 1.10.0/1.20.0/1.29.0/1.30.0、Python `mcp` 1.9.4/1.12.4/1.15.0/1.21.2/1.23.3/1.28.1/1.29.0/2.0.0、官方 Go SDK 1.4.0/1.5.0/1.6.1/1.7.0、官方 Rust SDK `rmcp` 1.0.0/1.1.0/1.8.0/2.2.0/3.0.1、官方 Ruby SDK `mcp` 0.25.0/1.0.0、官方 Java SDK `mcp` 1.0.0/1.1.3/2.0.0、官方 Kotlin SDK `kotlin-sdk-client-jvm` 0.12.0/0.13.0/0.14.0/0.15.0、官方 C# SDK `ModelContextProtocol.Core` 1.0.0/1.2.0/1.4.1/2.0.0 与官方 Swift SDK 0.11.0/0.12.1 使用隔离环境启动同一真实 bridge；Python 2.0 检查器同时适配官方 `httpx2`、snake_case DTO、数值 timeout 和二元 HTTP transport streams，Rust 3.x 检查器兼容可选 `server_info` 初始化元数据，并通过 stdio 与无状态 Streamable HTTP 生命周期，验证 `2024-11-05`/`2025-03-26`/`2025-06-18` 协商及 tools/resources/prompts。Swift 两版本的上游 transport/依赖声明无法在 Windows 原样编译，因此 Swift 矩阵只定义 Linux/macOS；截至 2026-07-30，九种官方 SDK 的当前稳定版均已纳入。独立 `MCP SDK Freshness` workflow 每周及手动查询 npm、PyPI、Go module proxy、crates.io、RubyGems、Maven Central、NuGet 和官方 Swift GitHub release，过滤预发布版本并校验 TypeScript 根依赖；请求支持 `HTTP(S)_PROXY`/`NO_PROXY`、20 秒 deadline、2 MiB 流式响应上限和三次有界重试，任一最新稳定版未进入矩阵即明确失败，后续只需对被发现的新版本完成兼容适配并扩展生命周期矩阵。
- MCP 已区分 `resources/list` 实际资源与 `resources/templates/list` URI 模板，支持 `ping`、有界 JSON-RPC batch/notification 语义；HTTP notification 返回无响应体的 `202 Accepted`。
- MCP 与桌面 IPC 都执行日志查询 `limit` 的 1..=1000 边界，日志搜索返回最近命中并按时间正序排列。
- 当 desktop IPC 不可用时，写工具已返回明确未执行错误；后续可考虑队列或离线计划。
- MCP 授权、HTTP 配置和审计已经分离为三个任务页；审计联合筛选、详情、刷新和有界 JSONL/SHA-256 导出已完成。`confirmWrites` 的逐次高风险调用审批也已覆盖全部写 scope，并具备 32 项队列上限、60 秒超时、锁屏 fail-closed 和 one-shot 决策语义。

### 测试与验证

本次审查已执行并通过的基础回归命令：

```bash
cargo fmt --all -- --check
cargo test --workspace -- --test-threads=4
cargo clippy --workspace --all-targets -- -D warnings
npm test -- --run
npm run test:workspace-ui
npm run build
```

`npm run build` 已把应用壳、文件管理器、会话设置、终端设置、密钥管理器、MCP Bridge、Sysmon 详情、日志管理、低频传输任务弹窗、端口转发弹窗、工作区辅助筛选面板、session/终端右键菜单、搜索、view 右键菜单/重命名弹窗、Quick Command 管理器、命令历史状态、SessionSummary cache 校验、串口分析器/窗口创建器、浏览器终端导出、终端 buffer/选择/在线搜索动作、xterm core/命令目录、WebGL 和 CSS 拆为真实 lazy chunk。当前主 JS 约 374.06 kB、文件管理器约 21.67 kB、会话设置约 40.30 kB、终端设置约 11.75 kB、密钥管理器约 48.52 kB、Sysmon 详情约 11.44 kB、日志管理约 11.23 kB、端口转发弹窗约 5.57 kB、传输任务弹窗约 4.27 kB、SessionSummary cache 校验约 6.96 kB、MCP Bridge 约 13.32 kB、独立终端窗口约 9.22 kB、终端 core JS 约 473.66 kB、WebGL JS 约 120.37 kB、主 CSS 约 153.02 kB、终端 CSS 约 3.93 kB；共享 session 搜索状态约 1.94 kB、工作区辅助筛选约 2.87 kB、命令历史状态约 1.77 kB、session/终端菜单约 4.84 kB、view 右键菜单约 4.63 kB、终端 buffer 约 1.39 kB、选择/在线搜索约 2.19 kB。主包与终端 chunk 均低于 500 kB，没有通过抬高阈值隐藏 warning。

终端浏览器回归覆盖 Unicode 11、write-only OSC 52、WebGL/DOM fallback、进程内屏幕恢复、查找、绝对/相对 buffer 行跳转、自由输入、候选区下置几何和 Insert/Normal 光标/输入隔离。终端文本导出覆盖同 session Primary/Mirror 精确路由、ANSI 去除、wrapped row、完整 buffer/精确 selection、空 selection 阻断、桌面/移动菜单和零终端写入；终端选择覆盖菜单 copy/select-all/clear、空选区错误、Insert/Local `Ctrl+Shift+C/A`、本地编辑/Normal 隔离、开启 SGR mouse reporting 后的真实矩形列选择、同 session view ID、桌面/移动几何和零鼠标/键盘写入。终端 buffer 回归在真实 XTerm 中区分 clear scrollback/screen/all，验证 alternate screen 禁用、Insert/Local 快捷键、Mirror 精确路由、紧凑桌面/移动布局和零后端写入。搜索、导出、选择和 buffer 模块均验证首次按需加载后的行为。

仓库内前端 Vitest 当前覆盖 70 个文件、399 项测试。`npm run test:workspace-ui` 会自行启动隔离 Vite/Chrome，从旧版六面板全开快照验证终端优先迁移，并覆盖左/右/底部停靠区同时显示、同区工具标签切换和非活动内容折叠、标题栏聚焦与跨区拖放、三向 pointer/键盘尺寸调整、双击复位、刷新持久化、关闭后空间归还、唯一资源树/历史筛选、四组顶层菜单及 SSH/Serial 精确禁用态、低频 pane 动作从标题栏迁到 view 右键菜单、四方向新分组子菜单、同步输入正反切换、顶部会话/日志共享搜索与键盘激活、发送区无伪控件、真实 Profile 启动目标的禁用/选择/保存/恢复、Profile 删除确认/后端单次调用/view 与快照收敛、终端设置单层标签、六协议会话设置双选择器/协议感知传输/无伪偏好写入、Jump Host 两跳删除后的 host/本地 Secret 草稿重排及保存重开持久化、MCP 三任务页互斥/联合审计筛选/详情/精确导出、关闭失败保留连接状态与诊断，以及 1440x900/390x844 桌面移动截图边界；上述非输入操作还会确认没有调用 `send_text`、`send_bytes` 或 `run_command`。其他工作区回归覆盖 v4 view 复制/别名/着色/重载/关闭恢复、同组排序、跨组拖放、独立窗口往返、标签循环、完整 view 右键菜单、分屏方向、精确移组、视图生命周期快捷键、可配置键盘模式、面板显隐、专注模式和主/独立窗口锁屏。命令补全覆盖 Quick/历史/参数排序、Tab 精确后缀、控制序列退让和主/独立窗口一致性；命令历史覆盖旧数组迁移、配置裁剪、发送记录、清除、关闭持久化后的进程内保留和 detached 读取；Quick Commands 覆盖管理/排序/取消/保存、Quick Bar 和插入/执行差异；串口分析器覆盖 delimiter/SLIP/COBS/Modbus framing、实时/Raw 日志源、证据视图、协议错误、分页、书签、重连诊断和导出。

该 workspace suite 还会把两个日志预览按反向顺序完成，并在 MCP HTTP 配置挂起时重复切换标签，直接覆盖弹窗请求门控。

已有单元测试覆盖：

- Host key alias 隔离、同 alias key mismatch、多算法 host key，以及编辑时的字段级并发三方合并/冲突和不可编辑密钥材料保留。
- Store open/close、profile upsert、MCP 空 grant 默认读、显式 read scope/session 过滤、write scope、send_text redaction/audit。
- Trigger contains/regex、七类动作前端字段往返、多动作后端 dispatch、自定义链接替换和声音/通知/高亮 runtime effect。
- 同步输入设置归一化、目标协议过滤、换行/显式批量发送前后缀变换、Telnet CRLF、FIFO 批次顺序、交互输入不重复包裹、部分失败和关闭后即时取消剩余目标。
- 终端序列化缓存的 UTF-8 字节上限、LRU 淘汰、事件 ID 上限、空屏恢复和防御性复制，以及 OSC 52 只写剪贴板、拒绝远端读取、权限 Promise 拒绝/同步异常降级。
- 当前终端查找的标准/WindTerm 快捷键识别、选中文本单行化和 UTF-16 长度边界、结果/溢出/非法表达式状态，以及菜单到焦点 pane 的事件分发。
- 终端文本 buffer 的 wrapped row 拼接、中间空行/尾部空行语义、UTF-8 精确字节计数、16 MiB 上限、空 buffer/selection 拒绝，以及 session/view/source 精确请求响应和无监听者超时。
- 终端只读选区/复制/全选/清除的精确 session/view/action 请求响应、错配与无监听者拒绝、Remote/Local `Ctrl+Shift+C/A` 模式隔离、块选择 Alt/force-selection 鼠标修饰转换，以及在线搜索选区优先/最近行回退、空值、2,048 Unicode 字符边界和结构化 URL。
- 终端 clear scrollback/screen/all 的本地控制序列、alternate screen 保护、精确 session/view/action 请求响应、错配与无监听者拒绝，以及 Remote/Local 的 Windows/Linux `Ctrl+L`/`Ctrl+Shift+L` 和 macOS Meta 变体。
- WindTerm `跳转到行` 的绝对/相对行解析、安全整数/范围边界、目标居中 viewport 计算、状态文案和菜单到唯一焦点 pane 的事件分发。
- 活动 pane 内按独立 view ID 的前后标签循环、首尾回绕、同 session 多 view 保留，以及 Remote `Alt+[`/`Alt+]`、Local/Normal `Ctrl+PgUp`/`Ctrl+PgDn` 快捷键解析与跨模式阻断；Remote/Local 关闭 view、全模式恢复 view 的默认快捷键及 Normal/Command 模式隔离。
- view 右键菜单对 32-view 复制上限、最后 view 关闭保护、关闭其他/右侧索引、现有/新分组移动、独立窗口、关闭 pane 和有效关闭历史的 capability 计算，以及精确 pane 深度限制和水平/垂直视觉标签到递归树轴向的统一映射。
- 自由输入的 Unicode 字符上限、跨平台换行原子提交、空/空格草稿、可编辑选区剪切和菜单到焦点 pane 的事件分发。
- 键盘模式非法值回退、默认 Insert 与 Normal 的 `Escape`/`i`/状态栏转换、竖线/方块/下划线光标映射、兼容 Local/本地编辑的 `Ctrl+Enter`、计数前缀、Vim 风格导航/搜索/可视选择命令，以及非 Insert 模式对未映射键和组合输入的消费。
- 命令补全损坏设置归一化、输入/退格/Ctrl+U/Ctrl+W 状态、未知控制序列暂停和换行恢复、命令/选项/子命令后缀、Quick/历史去重排序、来源开关、触发阈值与复杂 shell 语法退让。
- 命令历史容量/保留期归一化、旧字符串数组迁移、空/NUL/超长/重复命令拒绝、未来/过期时间戳处理、重复命令置顶、条数与 2 MiB UTF-8 总量上限，以及防御性 v2 快照。
- 串口分析器设置损坏值修复、Hex 分隔符校验、跨捕获分片且方向隔离的 delimiter/SLIP/COBS/Modbus framing、RFC 1055 END/ESC 解码、COBS 连续/空载荷/长度错误、Modbus 自动/手动静默阈值、标准 FC03/FC83 向量、CRC/地址/短帧错误、实时/Raw 日志源隔离、持久引用路径/范围/hash 校验、不可用/窗口外计数、历史精确导出、解码/线上双视图、定长完整/尾帧、空闲间隔、截断传播、4,096 帧环形显示边界、方向/Hex/ASCII/书签筛选、书签上限/不可变切换、Hex dump 和独立窗口路由校验。
- Quick Commands 的旧数组迁移、Unicode 名称/正文边界、NUL 清理、无效/重复 ID 修复、64 条上限、插入/执行 payload 和不可变上下排序。
- 工作区 pane/bar 的紧凑默认值、未经修改的 v1 全开布局到 v2 的迁移、定制 v1/旧直存快照恢复、逐字段损坏修复、幂等设置和不可变切换，以及不修改原状态的专注布局派生、同步输入状态栏保留和精确 `Alt+Enter` 识别。
- 工作区 session 筛选对名称、ID、分组、标签、协议、状态、SSH/Tmux/TCP/Telnet host/port、串口和 Shell 路径的匹配，以及历史命令大小写不敏感、折叠空白显示后的匹配与原始命令保留。
- 全局搜索与资源树共享会话匹配规则，日志结果覆盖跨 session 时间排序、所属会话上下文/命令 ID 匹配、80 条上限和 Unicode 预览边界。
- session 右键菜单的单行同步输入正/反动作、唯一拆分/移动入口，以及永久禁用和重复子菜单不会重新出现的结构约束。
- 终端设置的四槽启动 Profile ID 长度/NUL/类型修复、真实 session 选项去重、名称/协议标签和已删除目标的可见保留。
- workspace v1/v2/v3→v4 迁移、重复 node/view ID 修复、同 session 多 view、独立别名/颜色/键盘模式、精确激活/复制/关闭/排序/定点移动/拆分/合并、失效 session 收敛和带 view 身份/颜色/模式的 detach route 校验。
- 锁屏超时 `1..=1440` 归一化、绝对空闲 deadline、精确 WindTerm/Linux/macOS 快捷键，以及版本化跨窗口 marker 对损坏值的保守锁定与修复。
- Secret redaction。
- JSON 风格凭据、完整 Bearer token 脱敏，以及覆盖 SSH/Tmux credential metadata、本地路径、自动化载荷、传输路径和 raw shard ref 的 redacted session bundle。
- 运行时断线诊断跨 store reload 保留。
- 多跳 one-time host key 生命周期与 `AskEveryTime` 强制确认。
- MCP resource/template、opaque UTF-8 session/transfer ID 的资源 URI 编解码与非法路径拒绝、ping、batch、notification、HTTP `202`、日志 limit、stdio 超限恢复、慢请求总 deadline、HTTP/desktop IPC 连接限额拒绝与 permit 释放，以及 desktop endpoint 的文件/地址/Store/tokenRef 校验和 IPC 请求响应上限；官方 TypeScript SDK 四版本另以真实子进程覆盖 stdio 8 消息生命周期，并以真实 loopback bridge 覆盖 HTTP 9 请求序列。
- 隔离 OpenSSH 服务上的 TOFU、同地址 host key 变更阻断、`allowRotation` 后重新信任并保留轮换历史、公钥认证、PTY 命令、原生 SFTP 浏览/递归建目录/上传/rename/chmod/属性/远端复制/下载/递归删除、外部目录树递归上传（含空目录）、SFTP upload/download/remote-copy、SCP upload 和远端命令复制的 `.portmate-part` 偏移续传，以及 SCP download 的从 0 临时文件安全重试、限速 SFTP/SCP 上传取消后从 part 重试、SFTP subsystem 缺失/拒绝与 SCP command 拒绝失败状态、传输中 SSH 断开后重连续传，以及 local/dynamic/remote reverse tunnel 的流量统计、目标拒绝、错误状态和原 tunnel 恢复。
- 三 OpenSSH 服务上的两跳 Jump Host direct-tcpip 链、三端独立公钥身份筛选、两跳/目标独立 TOFU 持久化、末端 PTY、第一跳连接拒绝、第二跳 direct-tcpip 拒绝、第一跳/第二跳/目标静默握手超时、第二跳错误 identity 与目标 identity 耗尽的逐端点诊断，以及第二跳 host key 变更诊断。
- 用户态 russh password/keyboard-interactive 跳板与两台独立 OpenSSH 公钥端点组成的两种混合认证链，以及第一跳错误密码诊断和三端 host key 持久化。
- HTTP CONNECT/SOCKS5 对 TCP/Telnet 的真实转发、拒绝响应、无认证限制、目标域名交给代理解析、关闭代理时忽略残留端点、空代理端点、换行注入拒绝，以及 SOCKS5 非零保留字段/空绑定域名拒绝；混合认证 Jump Host 矩阵还覆盖经代理执行目标 host-key 扫描及 password/keyboard-interactive 正式连接。
- SSH 重连延迟/KeepAlive 的旧 Profile 默认值、非法阈值夹紧、KeepAlive 开关到 russh `keepalive_interval` 的映射、健康参数变化触发最新 Profile 重连代次更新，以及真实 OpenSSH 断线等待期间从 5,000 ms 缩短到 100 ms 后恢复 session 和原 tunnel。
- 独立真实 `ssh-agent` 与 OpenSSH 服务上的 agent 禁用、未过滤 offer、`IdentitiesOnly` 空白名单、显式指纹白名单，以及错误指纹不能被相同 comment/path 绕过；GSSAPI 未声明后的未过滤 agent fallback 另确认进入 libssh runtime，开启 forwarding 后远端 `ssh-add -L` 会返回本机 agent identity；同一 libssh runtime 还分别经 HTTP CONNECT/SOCKS5 loopback 完成连接并覆盖两类代理拒绝。
- OpenSSH PTY 上 lrzsz X/Y/ZModem 上传/下载、自动上传 `.portmate-part` 原子提交、相邻协议 stale-byte 隔离、raw TTY 恢复、XModem block padding 精确截断，以及 TCP loopback 下数据块/EOT 首个 ACK 丢失后的精确重传。
- Linux Docker SSH 服务端矩阵已在 OpenSSH Alpine 3.19/3.20/3.21、OpenSSH Debian Bookworm、OpenSSH Debian trixie、OpenSSH Ubuntu 24.04、OpenSSH Fedora 44、独立 GESFTPServer Ubuntu 和 Dropbear Alpine 3.20/3.21 上真实验证 SFTP/SCP 活动断线，并在同一容器的独立 SSH runtime 上交错验证 X/Y/ZModem 活动断线；Fedora 44 实际使用 OpenSSH 10.2p1 和 lrzsz 0.12.20。AsyncSSH 2.24.0 与 Paramiko 5.0.0 Debian Bookworm 另通过 PTY、三层健康、SFTP 文件生命周期、SFTP/SCP 上传下载和远端复制正常路径；AsyncSSH fixture 使用显式 stdin/stdout/stderr 转发和单一 channel 关闭路径，并在同一容器连续 12 轮完整正常路径中通过，避免多路自动 EOF 清理竞态掩盖客户端兼容性。独立 ProFTPD 1.3.8d `mod_sftp` Alpine 3.21、固定官方 OCI 摘要的 SFTPGo 2.6.6/2.7.5、固定官方 ZIP 摘要的 rclone 1.74.4、Debian bookworm/trixie 的 Erlang/OTP 25.2.3/27.3.4.1 `ssh_sftpd`、Debian trixie 的 Go `github.com/pkg/sftp` 1.13.6 与 Apache MINA SSHD 2.19.0 八个 SFTP-only 容器均使用 PortMate 的 russh SFTP 数据面完成上传、下载和远端复制，Apache MINA 还锁定重复 `MKDIR` 与非空 `RMDIR` 的扩展状态码语义；独立 fault server 还验证 SFTP subsystem 缺失/拒绝和 SCP command 拒绝均收敛为带协议诊断的失败任务；Alpine 的 lrzsz 从固定源码以 legacy C 模式构建，YModem 分配到 Debian、Debian trixie、Ubuntu 和 GESFTPServer Ubuntu（Alpine musl 的 `rb` 不发起 CRC 请求），因此该结果同时记录服务端和工具链边界。
- 传输限速等待不阻塞 async runtime，并能在 100 ms 轮询周期内响应取消。
- OpenSSH `MaxAuthTries 2` 下错误 key 优先导致认证耗尽、逐 identity 错误聚合，以及正确 key 前置后的成功连接。
- `socat` 虚拟 PTY 上的串口二进制收发、非 UTF-8 RX/TX 精确内存捕获、无探测字节的接收空闲超时、断线后切换到最新端口并动态缩短重连延迟、pending/connected 阶段关闭重连，以及设备不支持 DTR/RTS 时的兼容和拒绝边界。串口捕获单测覆盖 512 帧/1 MiB 环形边界、大帧截断标记、增量 reset、选中帧 JSONL 原子导出和 SHA-256 sidecar。
- SOCKS5 no-auth 协商、domain target 解析、非法认证方式和命令错误回复，以及服务端整体握手 deadline 和含空白字符 domain 的 address-type 错误回复。
- Client identity source/immutable ID 校验、重复 ID 拒绝、字段级并发三方合并/冲突、共享 secret 轮换隔离、Jump Host 删除阻断、全凭据引用计数，以及清理失败后已持久化 Profile 仍保持有效。
- Portable Stronghold Argon2id KDF 的密码/salt 绑定，以及 encrypted snapshot 无明文、错误主密码拒绝、正确密码重开、secret 写入/读取/删除、引用格式和 snapshot 缺失 salt 时不覆盖恢复线索的边界；主密码轮换覆盖错误当前密码/短密码/同密码拒绝、旧密码失效、新密码重开、secret 保留、旧密码重新解锁不覆盖新 provider、提交失败时旧 snapshot/provider 仍可用，以及第二个 stale 实例不能覆盖已轮换 snapshot。
- SSH/Tmux profile 凭据迁移覆盖五类 credential slot、SSH/Tmux 显式 scope、共享/legacy native alias、MCP HTTP/IPC 保留引用排除、全部读取预检、Stronghold 单批次提交/回滚/重开、native 精确读回、Profile 提交失败/不确定、源清理 warning、幂等目标、建连中源保留，以及会随迁移计划变化失效的预检 token。恢复测试覆盖 Prepared durable barrier、journal revision/CAS、Profile+journal 原子 checkpoint、结构化槽位 OLD/NEW/MIXED/缺失/交换分类、source missing、provider unavailable、内容 mismatch、Stronghold target unknown、cleanup checkpoint 失败和 corrupt journal 冻结；诊断回归验证结构化冲突证据、原子 JSON/checksum，以及 provider secret 和损坏 payload/state 均不泄漏。
- SessionStore CAS 覆盖 stale 第二实例、未更新 revision 的 kv 变更、损坏 SQLite 加载保护、重复加载不旋转 revision；SQLite mirror schema/trigger 的独立变化不制造逻辑快照冲突。
- 文件选择的单选/Ctrl/Command/Shift 状态转换、批次 `fail/overwrite/skip/rename` 冲突策略和路径逃逸拒绝，以及真实 OpenSSH SFTP 远端目录递归下载、空目录保留和冲突重命名。
- 传输状态本地化、生命周期消息过滤、失败 fallback 和可复制诊断文本，以及中文错误摘要的 UTF-8 安全截断。
- Remote tunnel listener 探测的 Linux `/proc`/`ss`、FreeBSD `sockstat`、macOS `lsof`、BSD `host.port` 和 unsupported 工具回退解析矩阵。
- 日志分片枚举/尾部 UTF-8 与 Hex 预览/批量删除的根目录约束、symlink 跳过、路径穿越拒绝、全量预验证和重复项去重，以及前端路径/格式筛选与全选合并。
- 解码前非 UTF-8 入站 raw、Telnet IAC/NVT 精确分片、48 线程共享路径追加的无重叠引用、带 SHA-256 的 v2 `bytesRef` 删除重建检测和旧引用兼容。
- Telnet 用户 text 的 CRLF wire、用户 bytes/modem 的 IAC doubling、协商 reply 的 outbound/control 无文本事件、每会话出站 lane、不可逆二进制结构化摘要、分片失败诊断，以及 transport 成功后 store 保存失败不触发重发且内存事件注解一致。
- Telnet/Raw TCP TLS 使用系统 CA 和 Server Name 默认拒绝自签名证书，显式启用不安全证书开关后完成握手并接收应用数据；真实 Ubuntu 24.04 `telnetd-ssl -z ssl` 容器验证加密 shell negotiation 与数据路径，Ubuntu 24.04 Ncat TLS echo 容器验证 Raw TCP 加密原始二进制回显，Debian Bookworm GnuTLS 3.7.9 echo 容器另验证 TLS 1.3 和保留 CRLF 的逐行回显边界。
- system/control 事件通道覆盖 direct/open/close 生命周期，验证脱敏 Text/JSONL 各恰好一次且 Raw 不变、shutdown drain，并锁定 inbound JSONL 先于由它触发的 system 诊断；Core 回归覆盖 wake 合并、4,096 条 outbox 上限和 worker 断开后的持续显式降级。SQLite mirror 会更新同 ID 事件的后补 annotations，同时不重复插入未变化历史。
- `.tar.gz` session bundle 的原子落盘、逐文件 manifest SHA-256、archive sidecar 校验、可独立验证的 Ed25519 detached signature、持久签名 seed 格式校验、已选日志附件的命名/哈希/数量/大小/symlink/截断/替换边界、脱敏与 raw/附件互斥和 `bytesRef` 范围读取；回归覆盖 `summary.lastLine`、SSH/Tmux credential metadata、本地路径、自动化载荷、传输路径、Sysmon 识别性标签、diagnostics 日志清单和 raw shard ref 泄漏，并确认未脱敏导出保持原值。
- 历史 Text/JSONL 分片全文搜索的大小写不敏感匹配、路径/行号/byte offset、全部/选中范围、raw 排除、查询长度、命中上限和路径穿越边界。
- 通用日志分片归档的流式读取、源文件保留、逐文件 manifest SHA-256、archive sidecar 校验、重复路径去重和路径穿越拒绝。
- profile 日志自动保留的旧配置兼容、模板归属约束、过期 mtime 删除、新分片和其他 profile 隔离，以及空日志根目录边界。
- Sysmon 旧摘要快照兼容、Linux/macOS/FreeBSD CPU/内存/负载解析、Windows PowerShell/CIM 编码命令与 marker JSON 解析、Windows 配置适配器和性能计数器名称不匹配时的地址仅行保留、Top 进程排序与 8 条边界、磁盘解析/挂载点去重与 16 条边界、Linux `/proc/net/dev`、macOS/FreeBSD `netstat -ibn` 和 Windows 性能计数器的每接口速率/重复行地址合并及 32 条边界、完整远端输出、真实本机 Linux `/proc`/`ps`/`df` 采样、本机 macOS/Windows 异步采样调度，以及本机命令非零退出/超时/4 MiB stdout/64 KiB stderr 边界、SQLite v3→v4 details 迁移和默认 120、允许 `1..=240` 的会话历史查询、时间戳去重排序、刷新即时归并及 CPU/内存/RX/TX 趋势量程。全应用 4 路 refresh permit 在采集与 Store 提交完成前持续持有，饱和时不会启动 worker 或产生事件；Linux `ps`/`df` 已从无界同步 `Command::output` 迁移到 12 秒、4 MiB/64 KiB 的异步双流采集，Unix 命令、输出流和子进程组共用同一 deadline，超时或读取失败会终止后代；本机地址会合并 `getifaddrs` 与每份 64 KiB 有界的 `/proc/net/if_inet6`、`/proc/net/fib_trie`、`/proc/net/route`，在仍没有可用地址时再使用受时限的 `hostname -I/-i` 和 `ip`/`ifconfig` 候选。每接口地址上限保留可用 IPv4、全局 IPv6、link-local、loopback 的优先顺序，多个低优先级地址不会挤掉可达 IP。
- 远端 Linux Sysmon 的网络地址回归覆盖并列的有界 `ip`、`ifconfig`、BusyBox 来源、`/proc/net/if_inet6`、`/proc/net/fib_trie`、`/proc/net/route` 和 `hostname -I/-i` 的本机回退；泛化的内核/hostname 地址会优先归属缺地址的默认路由接口，且即使其他接口已有地址也会补齐。仅在这些来源都没有可用地址时才查询 OpenWrt `ubus call network.interface dump` 的 `l3_device`/`device`/逻辑 `interface` 映射；即使 `/proc/net/dev` 没有对应接口计数，已解析地址仍会作为零速率接口保留。
- Tmux、远端 tunnel 健康探测和 Sysmon 共用的 SSH exec 捕获把 handle lock、channel open、exec 启动和结果读取纳入同一个调用方 deadline，并分别限制 stdout 4 MiB、stderr 64 KiB；精确上限可接受，越界分片会在写入前整体拒绝并保持已有缓冲区不变。
- Tmux control watcher 回归覆盖每 session 64 项和全应用 256 项 registry 容量、满表同 key 幂等/取消后替换、setup 前后容量变化的安装复核、停止后保留到退出，以及 256 个 lifecycle slot 饱和时在 SSH lookup 前零 registry 副作用拒绝。
- Session open 回归覆盖同 session 原子 single-flight、取消通知、permit 释放/复用，以及 64 路饱和时零 runtime/event/Store 状态副作用拒绝；既有真实 SSH 握手取消继续验证 Close 不会被占用的 lifecycle lane 阻塞。
- Profile 总量回归覆盖 10,000 项边界下已有 ID 更新、新 ID 拒绝、10,001 项 Store 拒绝，以及桌面 normalize 和 standalone MCP 快照加载的同一 fail-closed 规则。
- 日志保留检查 registry 回归覆盖同 Profile 天数覆盖、禁用/显式清理和一小时遗留项回收，确保配置 churn 不会留下乘法增长的缓存 key。

当前 Rust workspace 单元测试总数为 473：`portmate` 372、`portmate-kdf` 1、`portmate-core` 53、`portmate-mcp` 36、`libssh-rs` 2、`russh-sftp` 9，另有 2 项 `libssh-rs` 文档测试；`npm test` 另有 70 个文件、399 个前端 desktop-clean/display-formatter/menu-capability/transfer-capability/selection/presentation/log-shard/workspace/workspace-hotkey/workspace-view-context/workspace-panel/workspace-utility/context-menu/terminal-settings/session-settings/session-profile-helper/session-runtime/session-search/session-cache/screen-lock/detached-pane/trigger/tunnel/sync-input/terminal-state/terminal-search/terminal-export/terminal-buffer/terminal-selection/terminal-goto-line/terminal-key-mode/terminal-mouse/terminal-theme/command-history/Tmux/free-input/quick-command/OneKey/clipboard/secret-migration/SSH-health/TCP-health/Serial-health/Serial-capture/proxy/OpenSSH-config/Sysmon-history/MCP-audit/MCP-approval/AppImage-runtime/keyed-request-gate/file-navigation/window-geometry/workspace-window-route 单元测试。OpenSSH/socat/Stronghold/SQLite 集成测试在仓库内默认使用四个 libtest 线程，避免高核心数开发机过度并行造成虚假 wall-clock 超时，显式 `RUST_TEST_THREADS` 仍可覆盖。

主要缺口：

- 已有隔离 OpenSSH server、host key mismatch/`allowRotation`、MaxAuthTries/identity 顺序、真实 ssh-agent 策略/过滤和两跳 Jump Host 集成测试；第一/二跳连接拒绝、三段静默握手超时、逐跳独立 identity 拒绝、目标 identity 耗尽，以及 password/keyboard-interactive 到公钥端点的混合认证链均已覆盖；真实延迟 russh password 与 session-channel 回归验证认证、终端、辅助 exec 和 SFTP setup 总 deadline、当前 handle 断开和延迟服务端回调收尾。
- 已有 `socat` 虚拟串口 loopback 二进制收发、非 UTF-8 精确 RX/TX 捕获、无探测字节的接收空闲超时和 PTY 消失后的自动重连测试，覆盖切换到最新端口路径、等待期间从 2,500 ms 缩短到 200 ms、runtime ID 轮换、精确读错误诊断、重连期间拒绝写入、恢复后的双向 I/O，以及 connected 阶段关闭 reconnect 后直接断开；真实硬件和 modem 测试矩阵仍待补。
- Telnet/Raw TCP 已有 loopback mock 测试覆盖跨 read 分片的 IAC/TTYPE 子协商、Profile TTYPE、双向 BINARY 接受/拒绝/撤销、binary/NVT 数据差异、NAWS 协商前 resize、`0xff` 尺寸转义和连续 resize、NVT `CR NUL`/CRLF 与 EOF 孤立 CR、raw byte IAC 转义、Raw TCP 原样字节发送、内核 keepalive 自定义/关闭、旧 Profile 默认值与边界归一化、TLS 默认校验证书/自定义 Server Name/显式接受无效证书，以及断线自动重连状态恢复、运行中缩短重连延迟并切换端口、pending/connected 阶段关闭重连的收敛；18 项真实服务端矩阵现已覆盖 BusyBox Alpine 3.19/3.21、inetutils Debian/Ubuntu、telnetlib3 4.0.5 Debian Bookworm PTY shell、Twisted Conch 24.11.0 Debian Bookworm/26.4.0 Debian trixie 的真实 `/bin/sh` PTY bridge、ser2net 4.3.11 Debian Bookworm/4.6.4 Debian trixie 的 RFC2217-capable gensio Telnet accepter 和伪串口 PTY、netkit `telnetd-ssl` Ubuntu 24.04 的明文与 TLS shell，以及 Ncat 明文 echo Alpine/Ubuntu、Ncat TLS echo Ubuntu、GnuTLS 3.7.9 TLS line echo Debian Bookworm、Socat burst-close Alpine/Ubuntu 和 Socat close Alpine 七项 Raw TCP 组合；全部 Telnet shell 用例锁定协商、数据、resize 调用、退出和断线收敛，BusyBox/inetutils/Twisted/netkit 还以 `stty size` 实证 NAWS 已落到服务端 PTY，telnetlib3/ser2net 则因适配器不向子 PTY 传播 NAWS 而只承诺数据通路；ser2net 用例另锁定 PortMate 对 COM-PORT-OPTION 44 的明确拒绝和拒绝后的标准 Telnet 数据通路，更广真实设备仍待补。
- 已有 OpenSSH SFTP 浏览/写操作/传输、SFTP upload/download/remote-copy、SCP upload/远端命令复制五条偏移续传路径和 SCP download 从 0 安全重试、SFTP/SCP 取消后 retry、服务端拒写失败状态、活动 SSH 断开后重连续传、十服务端交错 SFTP/SCP 活动上传断线、AsyncSSH 2.24.0 与 Paramiko 5.0.0 PTY/健康/SFTP/SCP 正常路径、独立 ProFTPD `mod_sftp`、SFTPGo 2.6.6/2.7.5、rclone 1.74.4、Erlang/OTP 25.2.3/27.3.4.1 `ssh_sftpd`、Go `github.com/pkg/sftp` 1.13.6 与 Apache MINA SSHD 八个 SFTP-only 组合的上传/下载/远端复制、Apache MINA 的 v4+ `FileAlreadyExists`/`DirNotEmpty` 状态码和未知状态包解析、延迟 russh-sftp `LSTAT` 在途请求的单次发送/快速取消/subsystem 关闭/同 SSH 连接后续 exec 回归、静默 russh `scp -f` 下载与 remote-copy exec 对端的快速取消/空闲超时/有界 channel 清理及同连接后续 channel 回归、remote-copy marker 单调性/总大小一致性回归、lrzsz X/Y/ZModem 双向端到端、X/YModem 数据块与 EOT 的 ACK 丢失重传、静默 XModem 快速取消/CAN 和 transport 重连态旧 worker 快速失败测试；独立 SFTP 故障服务器另覆盖缺失远端源文件和只读目录的 `NoSuchFile`/`PermissionDenied` 语义，以及 subsystem/command 拒绝；更多服务端实现、更广异常状态、modem 物理串口/批量和工具变体矩阵仍待补。
- Transfer 队列回归覆盖每会话/全应用 5,000 项 Store 活动计数、整数溢出 fail-closed、runner semaphore 饱和时零 Store/cancellation 副作用，以及 queued worker 在 lane 被占用时仍能由取消通知退出、清理 handle 并释放 permit。
- 已有 russh/OpenSSH local/dynamic/remote reverse tunnel 端到端、三种模式目标拒绝后原 tunnel 恢复、remote 失败 channel 的 1 秒有界主动关闭、全应用连接槽饱和/释放和 remote-forward `u16` 端口边界、服务端撤销 remote forward 后被动探测/原端口重建、重复 cancel 被拒后的本地强制收敛、SSH channel 结束时按 session 清理旧 runtime、自动重连后按原 ID/标签/端口重建和单条端口冲突失败隔离，以及 SOCKS5 错误协议 loopback 测试；隔离 OpenSSH 上的真实 libssh runtime 另覆盖三种 tunnel 的目标拒绝、恢复、双向数据、metrics 和停止，并确认 remote Stop 后服务端监听不可再连接。setup deadline 单测覆盖成功、底层拒绝和永久 pending 的精确分类，真实延迟 russh direct-tcpip confirmation 回归覆盖超时与所属 session 断开。`sockstat`/`lsof`/BSD netstat 解析与失败工具回退已有单元矩阵，真实 FreeBSD/macOS SSH 主机仍待纳入集成环境。
- Tunnel Profile 单元回归覆盖 64 条定义、128/255 字符边界、重复 ID、非法 host/端口/mode shape、旧数据优先保留 enabled、disabled 历史回收、活动 runtime 上限、停止未知定义不追加、并发删除 Profile 时拒绝提交；前端回归覆盖严格端口解析、host/数量边界和 SessionSummary cache fail-closed，workspace UI 另验证真实 input 属性、非法值禁用态并输出 Tunnel 截图。
- 终端兼容、vttest、Tmux workflow、工作区、独立窗口和截图回归现已全部整理为仓库内 `playwright-core` suite，并复用已安装 Chrome；仓库已无直接连接 DevTools 协议的一次性浏览器控制脚本。workspace UI 覆盖旧多面板迁移、资源/文件/历史/发送三处同时停靠、同区工具标签切换和非活动内容折叠、标题栏聚焦和跨区拖放、三向尺寸调整/边界/双击复位与刷新持久化、关闭后终端全宽、文件列表/属性/日志预览反向响应、独立终端日志追赶/轮询失败保留、串口分析器 session 慢轮询串行化、tunnel 慢轮询串行化、MCP HTTP 慢加载防重入、关闭后成功 grant mutation 保留、Sysmon 跨会话反向响应、Profile 删除后 session/log 迟到响应失效、StrictMode 双启动列表与保存中 Profile 的补偿全量 hydration、启动 transfer 事件/MCP grant mutation 的独立域补偿收敛、真实筛选、精确上下文动作、Profile 删除确认/级联/view 与快照收敛、MCP 审批队列/去重/允许后 Escape 拒绝/桌面移动边界、无终端误写和聚焦截图；Tmux workflow 覆盖同步状态聚合、成功开关、失败回滚、刷新、attach/new-session、session/window 新建/重命名/确认关闭、pane activate/split/swap/resize/break/move/确认关闭、move 失败保留快照、window layout、同 SSH runtime 多 target control 并存/独立重启/精确停止/关闭全清理、推送静默刷新保留其他 target editor、旧 runtime stop 隔离、control refresh 与 mutation 的旧快照淘汰、关闭后 attach 迟到响应隔离及桌面/移动截图边界。
- Unicode 11、Serialize、write-only OSC 52、Profile 终端背景不透明度（透明度下 DOM renderer、100% 下 WebGL、原位保留 XTerm/buffer）和 WebGL fallback 已有浏览器回归；alternate screen/ANSI/truecolor/宽字符、双 pane PTY resize owner、SGR mouse、选择复制偏好、缓存恢复、真实 Vim/less/top PTY 和 6,000 行长日志已有可重复 Playwright 基线。新增 vttest 三版本×13 套件、四发行版 Vim/less/top/dialog 和 tmux 3.1c/3.3a/3.5a/3.7b 矩阵；原生 macOS/Windows 全屏程序及签名安装包 smoke test 仍需对应 runner/设备证据。

## 对照最终目标的完成度

| 目标域 | 当前状态 | 说明 |
| --- | --- | --- |
| 跨平台桌面框架 | 已实现 | Tauri v2 + React/TS + Rust 已成型。 |
| xterm 6 | 已实现 | `@xterm/xterm` 固定 `6.0.0`；当前焦点 pane 增量查找、绝对/相对 buffer 行跳转、精确 buffer/selection 文本导出、WindTerm clear scrollback/screen/all 与 alternate screen 保护、四套 Profile 主题及字体/字号/滚屏/主题原位热更新、Unicode 11、write-only OSC 52、进程内有界 Serialize/鼠标编码恢复、单一 PTY resize owner 及 WebGL→DOM fallback 已接入。 |
| WindTerm 风格工作台 | 大部分实现 | 终端优先的单层 view 标签/资源树默认布局、资源/文件/历史/发送左/右/底部多区停靠/同区工具标签切换和非活动内容折叠/标题栏同区重排与跨区拖放/v6 布局和有界尺寸持久化、三向 pointer/键盘边界调整与双击复位、主布局和菜单、最多 16 pane/8 层的递归水平/垂直分屏、每组最多 32 个独立 ID view、v1/v2/v3→v4 迁移、同 session view 复制/独立重命名/逐 view 着色、同组排序/跨组定点拖放/整组合并/四方向新分组/关闭与恢复、按 pane/view/session 精确路由的标签/终端右键菜单和终端文本/选区导出、带事务级联和独立窗口收敛的 Profile 删除、活动 pane 内按 view ID 首尾循环标签、可调且持久化的比例、pane/active/tab color 恢复、可配置且支持最多两段 chord/冲突校验的 WindTerm 分屏/方向焦点/关闭/zoom/Remote 与 Local/Normal 标签循环/视图关闭恢复快捷键、方向 pane 交换、保留 view 身份/颜色的 Tauri 独立窗口/返回、主密码/隐私降级锁屏、启动/空闲锁屏、启动会话策略、本地命令/选项/子命令/历史/Quick 补全及 xterm/CSS lazy chunk 已有。 |
| 同步输入 | 已实现 | 多 pane 去重广播、额外目标协议过滤、协议感知换行、目标间延迟、显式批量发送前后缀、FIFO、失败/即时取消反馈、明显目标计数和启动默认关闭均已接入，并有前端状态回归。 |
| SSH | 部分实现 | `russh` 0.62.4 PTY、密码、公钥、keyboard-interactive、ssh-agent、认证/终端 setup 总 deadline、Profile 级协议 KeepAlive 阈值、带可选认证的 HTTP CONNECT/SOCKS5、多跳 Jump Host 后端连接链路、每跳独立 secretRef/identityRef 和基础编辑可用；代理与 host-key 扫描路径一致且只作用于第一物理跳。两跳 OpenSSH direct-tcpip、三端独立 identity、逐跳 TOFU、连接拒绝、三段握手超时、逐端认证失败、第二跳 key mismatch、混合认证链、真实 ssh-agent 矩阵，以及 keepalive/exec/SFTP 三层健康和十四种服务端故障均已端到端覆盖。Linux 系统 libssh 已接通 GSSAPI 与 Profile Vault/System File public-key、无需精确指纹筛选的 before/after ssh-agent fallback、password、keyboard-interactive、none 混合顺序的 host key、认证、PTY/resize/exec/health 路径，并支持共享 HTTP CONNECT/SOCKS5、有限期预连接 socket ownership、有界 SSH agent forwarding、复用 russh 逐跳认证链的多跳 Jump Host、双后端 SFTP 文件浏览/属性/变更、upload/download/remote-copy、统一 exec channel 上的 SCP upload/download 与 remote-copy，以及统一 backend 上的 local/dynamic/remote reverse tunnel；Ubuntu 24.04、Debian bookworm、Debian trixie OpenSSH 以及 Apache MINA SSHD 2.19.0 + MIT Kerberos，再加 Ubuntu OpenSSH + Samba 4.19.5 AD-compatible KDC，均已覆盖成功、GSSAPI 优先、Host Key 拒绝、无票据、password 回退、服务端禁用、禁用时回退、SFTP subsystem 拒绝和 SFTP 操作拒绝九种场景；四个 OpenSSH 服务端验证真实 PTY resize，Apache MINA 验证交互 shell I/O，隔离 OpenSSH 另覆盖加密显式公钥回退、真实 ssh-agent fallback/forwarding、HTTP CONNECT/SOCKS5 成功和拒绝、三台服务端两跳 libssh PTY、完整 libssh SFTP 文件生命周期和三类 tunnel。显式 Agent identity/fingerprint 过滤仍由 russh 提供；Microsoft Active Directory GSSAPI 仍待实证。 |
| Host key 隔离 | 大部分实现 | profile alias、TOFU、mismatch block、known_hosts 导入导出、连接失败确认弹窗、一次性信任、多跳目标扫描/逐跳验证/确认、每跳自定义策略、Manager 分组筛选/批量删除/批量复制、字段安全并发编辑、`lastSeen` 持久化和 Store/Profile 镜像同步已有；剩余风险主要是原生 provider/文件系统跨平台故障矩阵。 |
| Bitvise 风格密钥管理 | 大部分实现 | keyring/secretRef、Host Key Manager scope/profile 分组过滤和批量删除/复制、host key 字段编辑、Client Key profile/source 搜索分组、跨 profile 批量复制/置顶/安全移除、私钥文件/粘贴导入、Agent identity 单条/批量添加、identity 字段编辑、Vault 私钥轮换、共享 secret 生命周期保护、Argon2id + IOTA Stronghold portable vault/fallback/主密码轮换，以及带预检 token、durable SQLite journal、原子 commit point、跨重启显式恢复、冲突冻结和安全诊断导出的 SSH/Tmux/TCP/Telnet profile 凭据双向迁移已有；跨平台 provider 故障矩阵待补。 |
| Shell/SSH/Telnet/TCP/Serial | 部分实现 | 基础连接读写、SSH/Tmux/TCP/Telnet 的 Profile 级 HTTP CONNECT/SOCKS5 与可选认证、SSH/Tmux 的重连延迟与协议 KeepAlive 阈值、Telnet 增量协商/NVT CR 编解码/Profile TTYPE/方向性 BINARY/NAWS/raw byte IAC 转义，以及 Telnet/Raw TCP 使用系统 CA、可选 Server Name 和显式不安全证书覆盖的 TLS、Telnet/Raw TCP loopback、TCP/Telnet 的 Profile 级重连延迟与 OS keepalive、Serial 的重连延迟/无探测接收空闲阈值/精确有界 RX-TX 捕获/方向与内容过滤/原子 JSONL 导出、独立分析窗口/capture/delimiter/fixed/gap/SLIP/COBS/Modbus framing/解码与线上证据/协议错误/书签/分页/实时与 4096 帧/8 MiB Raw 日志历史/源帧导出/重连诊断、SSH/TCP/Telnet/Serial 重连加载最新 Profile 并拒绝过期尝试、TCP/Telnet/Serial pending/connected 阶段禁用收敛、虚拟串口切换最新端口自动重连、runtime 最近断开原因可见、break、DTR/RTS 和 hex 字节发送可用；更广物理设备和跨平台矩阵待补。 |
| Tmux | 大部分实现 | list/attach/new-session、按 window 聚合的 pane inspection、经 SSH exec 确认的 `synchronize-panes` 开关、完整 session/window/pane mutation、五种布局，以及同一 SSH runtime 上多个 tmux session 的独立 control-mode 监听/刷新/停止可用；watcher 容量、setup 复核、同 key 幂等和有界清理已覆盖。真实 tmux 3.1c/3.3a/3.5a/3.7b 矩阵验证状态解析、空格名称、同步、layout/mutation 和 control 通知；跨原生 OS 仍由 runner/安装 smoke test 补证。 |
| SFTP/SCP | 部分实现 | 原生 SFTP 和 SCP、10 秒 SFTP/subsystem 与 SCP/remote-copy exec setup 总 deadline、SFTP 的 20 秒单请求上限和运行期 100 ms 不重启请求的取消轮询/subsystem 收尾、SCP I/O 的 100 ms 取消轮询/60 秒空闲 deadline/有界 channel 清理、远端命令复制的 100 ms 取消轮询/60 秒有效进度空闲 deadline/300 秒总 deadline/4 MiB + 64 KiB 输出边界/marker 一致性校验/有界 channel 收尾、双栏、多选/连选/全选、批量删除、rename、chmod、属性查看、面板间及原生外部文件/目录树拖放、远端目录递归下载、空目录保留、安全批次规划、四种冲突策略、retry、速度、local/SFTP/SCP 分块进度与取消、profile 级异步可取消限速、local/SFTP upload/download、SCP upload 断点续传和 SCP download 从 0 安全重试、远端命令复制大小标记/目标大小轮询进度/取消和 `.portmate-part` 续传、5,000 项 Store/runner 双层活动上限、等待 lane 的通知式取消、后台串行队列调度、全量队列视图、批量取消/重试和失败诊断展示已有；真实 OpenSSH 递归上传/下载和冲突重命名、延迟 russh-sftp 请求、静默 russh SCP 下载/remote-copy、十服务端交错 SFTP/SCP 活动上传断线（含 Fedora 44 OpenSSH 10.2p1 与独立 GESFTPServer SFTP 实现）、AsyncSSH 2.24.0 与 Paramiko 5.0.0 PTY/健康/SFTP/SCP 正常路径、独立 ProFTPD `mod_sftp`、SFTPGo 2.6.6/2.7.5、rclone 1.74.4、Erlang/OTP 25.2.3/27.3.4.1 `ssh_sftpd`、Go `github.com/pkg/sftp` 1.13.6 与 Apache MINA SSHD SFTP-only 上传/下载/远端复制，以及独立 fault server 的 SFTP subsystem 缺失/拒绝、SFTP v3-v6 全部 26 个服务端错误状态码 `4`/`5`/`8..31`、未知码 `99`、畸形响应类型、零长度帧、字段缺失的状态响应、截断响应、超限声明长度与错误 request ID 五秒内保留精确诊断和 SCP command 拒绝等三十八种传输失败均已覆盖；合法反序响应另有按 request ID 路由单测，接收路径会在分配声明载荷前执行配置上限，规范限定为客户端本地产生的 `6`/`7` 不伪造成服务端响应。更多服务端实现仍待补。 |
| X/Y/ZModem | 部分实现 | 三者都有实现，块级进度与取消已接入；OpenSSH PTY + lrzsz 六方向传输、raw TTY、READY/DONE 门控、XModem 精确长度、静默对端取消后 CAN/worker 清理和 transport 重连态断线失败已覆盖；OpenSSH Alpine 3.19/3.20/3.21、Debian Bookworm、Debian trixie、Ubuntu 24.04、Fedora 44、GESFTPServer Ubuntu 和 Dropbear Alpine 3.20/3.21 十服务端的独立 SSH runtime 活动断线矩阵也已通过，包含失败收敛、部分进度、最终文件未提交与 `.portmate-part` 清理。物理串口、批量和工具变体矩阵待补。 |
| 隧道 | 大部分实现 | local/remote/dynamic、运行中列表、停止入口、连接数/字节/最后错误、SOCKS5 5 秒握手 deadline、三模式 10 秒连接 setup 边界、每 Profile 64 条有界定义、全应用 256 活动 runtime/256 活动客户端上限、字段/端口/mode shape 统一验证、旧 Store/cache/重连 fail-closed、disabled 历史回收、并发 Profile 删除补偿、channel-open 超时断线清理、remote reverse 目标失败/饱和/无路由/非法端口的 1 秒 channel 收尾、监听器终止、Linux/FreeBSD/macOS remote forward 被动探测、撤销后重建、cancel 失败本地收敛、SSH 断线清理和重连后原规格恢复已接入；russh/OpenSSH 与 libssh/OpenSSH 三模式的目标拒绝、恢复、双向数据、metrics 和停止均已覆盖，libssh Stop 另确认服务端监听已撤销。真实 BSD/macOS 主机和更广服务端矩阵待补。 |
| Sysmon | 大部分实现 | 本机 Linux/macOS/Windows 与 SSH/Tmux Linux/macOS/FreeBSD/Windows 的 CPU、内存、uptime、吞吐、Top 进程、磁盘和每接口指标已进入有界快照、SQLite、MCP、四标签工作窗口及当前会话常驻侧栏；历史趋势、10 秒采样、请求代际/并发限制、断线停止和失败保留已覆盖。Linux/BusyBox/OpenWrt 地址回退及 Windows PowerShell/CIM marker 均有解析矩阵；仍需真实 macOS/FreeBSD/Windows SSH 主机和其他 BSD 证据。 |
| 日志 | 大部分实现 | 结构化 events/SQLite、显式命令与入站事件 UUID 关联、带毫秒/方向/session/pane/command 的逐行 Text、双向精确 transport raw、Telnet reply/modem control、system Text/JSONL sink、每会话出站 lane、共享路径串行追加、SHA-256 v2 `bytesRef`、预览/筛选/搜索/清理/保留/归档，以及带 Ed25519 detached signature、可选 raw 和有界已选日志附件的脱敏 session bundle 已有；完整跨平台真实文件系统/keyring 故障矩阵仍待补。 |
| 触发器 | 已实现 | 多条 contains/regex 规则、多动作编辑、高亮、通知、时间线、本地命令、发送文本、自定义链接和声音均有模型、运行时 dispatch 与回归覆盖；Profile/编辑器/缓存/运行时共享 64 条规则、每条 16 个动作和字段长度/合法性边界，send-text 有 32 条顺序批次/8 批并发和 transport 代际隔离，本地命令另有 4 路并发、30 秒总时限、双流各 64 KiB、饱和拒绝诊断及显式进程清理边界。 |
| MCP stdio | 已实现 | bridge、tools/resources/prompts、opaque UTF-8 session/transfer ID 的资源 URI 路径段编码/严格解码、空 grant 默认只读与显式 read scope/session 过滤、live IPC 二次授权/命令白名单、全部写 scope 的可选一次性桌面审批、32 项 pending 上限、60 秒 fail-closed/one-shot 响应、脱敏审批事件和副作用前授权审计、1 MiB 可恢复输入边界、128 项 batch 上限、64 MiB 响应序列化边界、严格 ID/params envelope、逐 envelope Store/endpoint 刷新、endpoint 信任边界和桌面 IPC 的 64 连接/5 秒 I/O 边界已有；官方 TypeScript SDK 四版本已对开发二进制完成真实 8 消息生命周期及子进程退出，九种语言的全部矩阵也已逐包验证 DEB/RPM/AppImage 内置 sidecar；目标平台 sidecar 会随桌面安装包交付。 |
| MCP HTTP | 部分实现 | `portmate-mcp --http` 支持 loopback JSON-RPC、Origin 校验、Bearer/X-Token、本地 keyring token、无状态 Streamable HTTP、GET SSE、纯 SSE POST、严格 HTTP framing、容量和 deadline；官方 TypeScript SDK 1.10.0/1.20.0/1.29.0/1.30.0、Python 1.9.4/1.12.4/1.15.0/1.21.2/1.23.3/1.28.1/1.29.0/2.0.0、Go 1.4.0/1.5.0/1.6.1/1.7.0、Rust `rmcp` 1.0.0/1.1.0/1.8.0/2.2.0/3.0.1、Ruby 0.25.0/1.0.0、Java 1.0.0/1.1.3/2.0.0、Kotlin 0.12.0/0.13.0/0.14.0/0.15.0、C# 1.0.0/1.2.0/1.4.1/2.0.0 和 Swift 0.11.0/0.12.1 的 HTTP/stdio 生命周期已通过；Swift 受上游限制只定义 Linux/macOS 矩阵。截至 2026-07-30，九种官方 SDK 的当前稳定版均已覆盖；每周/手动 freshness workflow 会从八类官方注册表自动发现未覆盖的新稳定版，支持企业代理并对瞬时失败有界重试。桌面授权/HTTP/审计任务页、请求门控和有界审计导出已有。 |
| 测试体系 | 部分实现 | Rust workspace 与前端单测、终端/Tmux/workspace Playwright、Linux Tauri/WebKit 首帧像素门禁、vttest 三版本×13 套件、四发行版全屏程序、tmux 3.1c/3.3a/3.5a/3.7b、OpenSSH 9.6/9.7/9.9 + Debian bookworm + Debian trixie + Ubuntu 24.04 + Fedora 44 OpenSSH 10.2 + 独立 GESFTPServer + Dropbear + AsyncSSH 2.24.0 + Paramiko 5.0.0 的正常路径和十例交错 SFTP/SCP 活动上传断线、十四种 SSH 健康故障、三十八种 SFTP/SCP 传输故障、Ubuntu 24.04 OpenSSH 9.6 + Debian bookworm OpenSSH 9.2 + Debian trixie OpenSSH 10.0 + Apache MINA SSHD 2.19.0 的四服务端 MIT Kerberos GSSAPI，以及 Ubuntu OpenSSH + Samba 4.19.5 AD-compatible KDC，五个组合均覆盖成功/GSSAPI 优先/Host Key 拒绝/无票据/password 回退/服务端禁用/禁用时回退/SFTP subsystem 拒绝/SFTP 目录操作拒绝九场景、BusyBox Alpine 3.19/3.21、inetutils Debian/Ubuntu、telnetlib3 4.0.5 Debian Bookworm、Twisted Conch 24.11.0 Debian Bookworm/26.4.0 Debian trixie、ser2net Debian bookworm/trixie 与 netkit `telnetd-ssl` Ubuntu 24.04 Telnet、Ncat/Socat Alpine/Ubuntu 与 GnuTLS Debian TCP、TypeScript MCP 四版本、Python MCP 八版本、Go MCP 四版本、Rust MCP 五版本、Ruby MCP 双版本、Java MCP 三版本、Kotlin MCP 四版本、C# MCP 四版本和 Swift MCP 双版本均已自动化；DEB/RPM/AppImage 的主程序元数据、资源/权限/链接及包内 sidecar 全语言矩阵已逐包通过。Windows MSI/NSIS administrative extraction 与静默安装/卸载检查、macOS app/DMG metadata 与只读挂载检查已定义并接入对应 Native CI job，共享布局边界有跨平台单测；Ubuntu job 已在隔离 D-Bus + Xvfb 中接入原生首帧门禁，但尚无远端 runner 的成功记录。所有 Docker 矩阵支持三次镜像构建重试和显式、缺镜像即失败的离线缓存模式。Linux CI 还会对系统 libssh 构建执行 `ssh_userauth_gssapi` 符号和 GSSAPI 动态依赖双重检查。`Native CI` 定义 Ubuntu/Windows/macOS 测试、Clippy、TypeScript/Python/Go/Rust/Ruby/Java/Kotlin/C#/Swift SDK 和 Tauri bundle，以及 Linux 全兼容矩阵；仍需 CI 实际成功记录、签名安装包 smoke test、真实远端 OS/物理设备证据。 |

## 下一阶段目标

### P0：把 alpha 变成稳定可日用版本

1. Client identity 字段编辑、密钥轮换、引用计数生命周期管理、OS keyring 不可用时的 IOTA Stronghold portable vault/fallback、主密码轮换，以及带 durable journal/跨重启核对/安全 conflict 诊断导出的 SSH/Tmux profile 凭据双向批量迁移已完成；Linux Secret Service 的真实跨进程 CRUD、provider unavailable 和 locked/prompt-denied 已完成，Windows Credential Manager/macOS Keychain 同一 CRUD probe 已接入 Native CI；继续补对应 runner 成功证据、Windows/macOS locked/denied provider 和三平台 Stronghold 故障注入矩阵。
2. Jump Host password/keyboard-interactive 混合认证、连接拒绝、三段握手超时与逐端 identity 失败诊断已覆盖。
3. remote forward 服务端撤销的被动探测/原端口重建、cancel 失败后的本地收敛，以及远端命令型传输失败详情、部分进度、事件摘要和复制诊断均已完成；继续扩展服务端故障矩阵。
4. SFTP/SCP 的 OpenSSH Alpine 3.19/3.20/3.21、Debian bookworm、Debian trixie、Ubuntu 24.04、Fedora 44、独立 GESFTPServer Ubuntu、Dropbear Alpine 3.20/3.21、AsyncSSH 2.24.0 与 Paramiko 5.0.0 Debian Bookworm 正常矩阵、十组交错的 SFTP/SCP 活动上传断线、SFTP subsystem 缺失/拒绝与 SCP command 拒绝、缺失远端源文件/只读目录的 `NoSuchFile`/`PermissionDenied` 语义、全部 26 个标准服务端错误码、未知码 `99`、畸形响应类型、零长度帧、字段级畸形状态响应、截断响应、超限声明长度和错误 request ID 的快速失败与精确诊断、合法反序响应的 request ID 路由、SSH health 的 ping/exec/SFTP 故障注入，以及 BusyBox Alpine 3.19/3.21、inetutils Debian/Ubuntu、telnetlib3 4.0.5 Debian Bookworm、Twisted Conch 24.11.0 Debian Bookworm/26.4.0 Debian trixie、ser2net Debian bookworm/trixie RFC2217-capable Telnet、netkit `telnetd-ssl` Ubuntu 24.04 明文/TLS Telnet、Ncat 明文/TLS 与 Socat Alpine/Ubuntu TCP、GnuTLS Debian TLS TCP 已完成；SSH 容器就绪会分别等待 Docker 动态端口发布和完整协议 banner，TCP/Telnet 通过容器内 LISTEN 状态检查且不会以裸连接消费被测会话，Telnet shell 用例会先确认首轮 negotiation reply 再发送应用数据，严格 NAWS 用例还会等待协商完成并以服务端 `stty size` 验证 PTY resize，避免冷启动竞态把服务端兼容性与测试控制面故障混为一谈。继续补 modem 物理串口和更多 SSH 服务端实现。
5. SSH/TCP/Telnet/Serial 的最新 Profile 重连、动态延迟、KeepAlive/idle、runtime 断线诊断和 SSH 三层健康检测已完成；SSH runtime/PTY/exec/health、SFTP 文件操作与三类传输、SCP upload/download/remote-copy、三类 tunnel、HTTP CONNECT/SOCKS5 Proxy、Agent forwarding 和复用 russh 逐跳认证链的 Jump Host 已迁入 backend 边界，GSSAPI 与 Profile Vault/System File public-key、无需精确指纹筛选的 before/after ssh-agent fallback、password、keyboard-interactive、none 混合顺序已接入 Linux 系统 libssh，并排除不支持动态 resize 的 OpenSSH native mux。Ubuntu 24.04、Debian bookworm、Debian trixie OpenSSH 加 Apache MINA SSHD 2.19.0 + MIT Kerberos，以及 Ubuntu OpenSSH + Samba 4.19.5 AD-compatible KDC 的五组合真实九场景矩阵已完成；三台隔离 OpenSSH 的两跳 libssh PTY/有界清理也已完成；下一步补 Microsoft Active Directory 实证，同时补 Serial 物理设备和真实远端 OS 故障证据。

### P1：补齐 WindTerm/Bitvise 级工作流

1. 文件管理器多选、可配置冲突策略和远端目录递归下载已完成；继续扩展 SFTP/SCP 服务故障矩阵和跨平台路径边界。
2. pane view group、启动自动连接、session/逐 view 标签颜色、workspace restore、v1/v2/v3→v4 自动迁移、同 session view duplicate/独立 rename、view 同组排序/跨组定点拖放/整组合并/四方向新分组/关闭与恢复、按 pane/view/session 精确路由的完整标签右键菜单、活动 pane 内不跳过同 session view 的前后标签循环、任意递归嵌套分屏、保留 view 身份/颜色的独立 Tauri 窗口/返回、跨主/独立窗口锁屏、可持久化的多区域工作停靠/status 显示开关、可逆专注模式，以及支持最多两段 chord 的可配置 WindTerm 分屏创建/方向焦点/关闭/zoom/Remote 与 Local/Normal 标签循环/视图关闭恢复快捷键和方向 pane 交换已完成。
3. 同步输入正式化已完成：多 pane 去重广播、协议过滤、换行策略、延迟、显式批量发送前后缀、FIFO、失败/即时取消反馈和明显目标计数均已接入；为避免误广播，开关不跨启动保留。
4. FreeType 风格自由输入、按 view 持久化的 Remote/Local/Normal/Command 键盘模式、scheme 风格本地命令补全、Quick Commands 和独立 OneKeys 凭据管理已完成：焦点 pane 本地有界多行编辑、Vim 风格 scrollback 导航/选择/复制、绝对/相对 buffer 行跳转、非 Remote 输入阻断、终端换行、查找互斥、同步输入复用、本地命令/选项/子命令/历史/Quick 后缀补全、有界命令管理/排序/持久化、Quick Bar 插入或执行，以及加密 OneKey Secret、会话绑定、自有公钥身份、手动敏感字段发送、仅传 ID 的 SSH 登录弹窗选择和经后端重验证的终端用户名/密码提示补全均已接入。
5. 串口工具增强：精确有界 Hex/ASCII viewer、收发过滤、JSONL + SHA-256 导出、独立 Tauri 分析窗口、capture/delimiter/fixed/gap framing、RFC 1055 SLIP/COBS/Modbus RTU 解码、解码/线上证据切换、协议错误、分页、书签、重连状态，以及显式 Raw 日志支撑的 4,096 帧/8 MiB 持久历史均已完成；下一步补更广物理设备和跨平台矩阵。
6. 密钥管理器继续增强：portable vault 创建/解锁/锁定、主密码轮换、Client identity 字段编辑、密钥轮换、底层 secret 生命周期管理、SSH/Tmux profile 凭据双向批量迁移、migration journal、恢复/重载 UX 和人工 conflict 诊断导出已完成；下一步补跨平台 provider 回归。
7. Tmux 完整管理和 control-mode workflow 已完成，真实 tmux 3.1c/3.3a/3.5a/3.7b server/control 矩阵也已覆盖；下一步只保留原生 OS/发行版扩展与安装后全屏 smoke test。

### P2：日志、诊断和 MCP 产品化

1. append-only raw/text/jsonl 分片的安全枚举、受限预览、筛选、Text/JSONL 历史全文查询、批量清理 UI、通用归档、profile 自动保留期、双向精确 transport 字节/v2 引用、出站顺序、逐行毫秒元数据、显式命令 UUID 关联和 system Text/JSONL sink 已完成。
2. `export_session_bundle` 的桌面 `.tar.gz` 交付包、逐文件/整包校验、平台/store 诊断、默认脱敏、显式 raw、Ed25519 detached signature 和日志管理器已选分片附件策略已完成。
3. MCP 官方 TypeScript SDK 1.10.0/1.20.0/1.29.0/1.30.0、Python SDK 1.9.4/1.12.4/1.15.0/1.21.2/1.23.3/1.28.1/1.29.0/2.0.0、Go SDK 1.4.0/1.5.0/1.6.1/1.7.0、Rust SDK `rmcp` 1.0.0/1.1.0/1.8.0/2.2.0/3.0.1、Ruby SDK 0.25.0/1.0.0、Java SDK 1.0.0/1.1.3/2.0.0、Kotlin SDK 0.12.0/0.13.0/0.14.0/0.15.0、C# SDK 1.0.0/1.2.0/1.4.1/2.0.0 与 Swift SDK 0.11.0/0.12.1 的 stdio/HTTP 矩阵已纳入回归；截至 2026-07-30 已覆盖九种官方 SDK 的当前稳定版。注册表 freshness 检查已接入每周和手动 CI，未来稳定版遗漏会自动失败并指出应扩展的矩阵。
4. Sysmon 的跨平台采样、四标签窗口、常驻当前会话侧栏、历史趋势、10 秒刷新和结构化持久化已完成；继续补真实 macOS/FreeBSD/Windows SSH 主机与其他 BSD。
5. 终端/vttest/Tmux/workspace 浏览器回归已全部迁入 Playwright，vttest 三版本×13 套件、四发行版全屏程序和 tmux 四版本矩阵已完成；Docker PTY 首字节启动、程序执行和容器/CLI 收尾使用独立边界，慢控制面不会占用全屏程序 deadline。下一步由 macOS/Windows runner 与安装 smoke test 补原生证据。

### P3：架构整理与发布准备

ProFTPD 1.3.8d `mod_sftp` Alpine 3.21、SFTPGo 2.6.6/2.7.5、rclone 1.74.4、Erlang/OTP 25.2.3/27.3.4.1 `ssh_sftpd`、Go `github.com/pkg/sftp` 1.13.6 Debian trixie 与 Apache MINA SSHD 2.19.0 八个 SFTP-only 组合均已通过 PortMate russh SFTP 上传、下载和远端复制验证；Apache MINA 还覆盖协商 v3 后返回 v4+ 状态码的真实行为，独立 fault server 则验证全部标准服务端错误码、未知码 `99`、畸形响应类型、零长度帧、字段级畸形状态响应、截断响应、超限声明长度和错误 request ID 都不会等到请求超时，合法反序响应另由 `russh-sftp` 单测确认按 request ID 路由，客户端接收上限会在分配声明载荷前执行。SSH 服务端矩阵现有 20 个实现/发行版组合，其中 12 个通过 PTY、三层健康、SFTP 和 SCP 正常路径，10 个 OpenSSH/Dropbear 组合另通过 SFTP/SCP 与 X/Y/ZModem 活动断线矩阵。

1. `lib.rs` 已累计减少约 42,000 行，当前仅保留 346 行 module wiring、公开重导出、共享常量和 `tests/mod.rs` 入口；原 657 行共享测试夹具及子模块注册已整体迁入 `tests/mod.rs`。主文件不再直接拥有 Tauri command，跨域 IPC 请求/响应类型已迁入 `command_types.rs` 并由根模块保持公开重导出，应用初始化/command 注册/退出收尾也已迁入 `app_bootstrap.rs`，并按以下边界完成拆分：
   - transport/runtime：`tmux_commands.rs`、`tmux_protocol.rs`、`tmux_runtime.rs`、`telnet_protocol.rs`、`proxy_protocol.rs`、`tcp_transport.rs`、`tcp_reconnect.rs`、`serial_commands.rs`、`serial_transport.rs`、`shell_transport.rs`、`ssh_auxiliary.rs`、`ssh_connection.rs`、`ssh_connection_steps.rs`、`ssh_handler.rs`、`ssh_session_lifecycle.rs`、`ssh_transport.rs`、`ssh_transport_setup.rs`、`ssh_libssh_transport.rs`、`ssh_backend.rs`、`ssh_exec.rs`、`ssh_runtime.rs`、`ssh_tunnel.rs`、`ssh_tunnel_health.rs`、`ssh_tunnel_io.rs`、`ssh_tunnel_lifecycle.rs`、`ssh_tunnel_store.rs`、`tunnel_commands.rs` 和 `serial_capture.rs` 承接 Tmux/Serial/tunnel 桌面命令适配、Tmux control watcher 注册/容量/事件循环/取消以及状态读取/变更编排、协议状态机、TCP/Telnet/Serial runtime、Shell PTY/子进程 runtime、SSH 正式 handler/host-key 接受/remote-forward 分发、socket 建立、直连/多跳目标链与 Jump Host 策略/凭据继承、terminal 与 auxiliary exec/SFTP setup、跨域 auxiliary capacity/lease、russh/libssh session/channel/direct-tcpip 统一边界、共享 exec capture/PowerShell 编码、SSH channel reader/断开诊断/代际重连与安装补偿、local/remote/dynamic tunnel runtime/metrics/健康恢复/停止生命周期/Profile 持久化与数据 pipe、端口打开与读循环、最新 Profile 自动重连、串口线制映射与 capture。
   - security/profile：`ssh_authentication.rs`、`ssh_security.rs`、`ssh_host_key_commands.rs`、`ssh_identity_commands.rs`、`ssh_host_key_scan.rs`、`ssh_health.rs`、`one_key_commands.rs`、`one_key_prompt.rs`、`one_key_runtime.rs`、`profile_commands.rs`、`profile_normalization.rs` 和 `profile_security.rs` 承接 libssh/russh 认证顺序、GSSAPI backend 选择、password/keyboard-interactive/agent/private-key 认证与密钥加载、认证超时错误，SSH 信任、完整 Host Key 命令/Store mirror/并发字段合并、identity/agent 枚举/更新/轮换/删除、SSH/Tmux/代理 Profile 访问、Client Identity 合并与 Secret 引用规范化/全局计数、直连/多跳扫描、健康检查、OneKey CRUD/发送/提示复核/摘要/登录凭据/身份克隆/Secret 生命周期，以及 Profile 保存事务、expected/current/incoming 字段合并、代理密码更新和归一化。
   - storage/vault：共享核心 `store/events.rs`、`store/exports.rs`、`store/histories.rs`、`store/security.rs`、`store/sessions.rs`、`store_system_events.rs`，以及桌面端 `state_snapshot.rs`、`sqlite_schema.rs`、`sqlite_mirror.rs`、`sqlite_store.rs`、`store_normalization.rs`、`store_persistence.rs`、`store_transactions.rs`、`migration_types.rs`、`migration_planning.rs`、`migration_runtime.rs`、`migration_diagnostics.rs`、`migration_journal_store.rs`、`migration_recovery.rs`、`secret_commands.rs`、`secret_provider.rs`、`vault_commands.rs` 和 `portable_vault.rs` 承接有界辅助历史归一化、runtime-only system-event outbox、Store/SQLite/Stronghold/keyring 的持久化、Secret/Vault/迁移命令、copy-on-write/tracked-event 事务、提交后核验、一致性、迁移私有载荷/计划/投影/事务执行/恢复、journal 校验/读写/pending 门禁以及受限诊断模型/生成/原子导出。
   - MCP：`mcp_commands.rs`、`mcp_control.rs`、`mcp_ipc.rs`、`mcp_authorization.rs` 和 `mcp_execution.rs` 承接桌面 command adapters、grant/approval、私有 IPC、授权审计与工具执行。
   - transfer：`transfer_commands.rs`、`transfer_runtime.rs`、`file_commands.rs`、`file_metadata.rs`、`file_operation_paths.rs`、`file_operations.rs`、`file_batch.rs`、`external_drop_planning.rs`、`file_transfer.rs`、`remote_copy.rs`、`sftp_backend.rs`、`sftp_paths.rs`、`sftp_transfer.rs`、`scp_protocol.rs`、`modem_remote.rs`、`modem_transfer.rs`、`modem_protocol.rs`、`modem_xmodem.rs`、`modem_ymodem.rs` 和 `modem_zmodem.rs` 承接传输完整桌面命令适配、文件桌面命令适配、共享本地/SFTP 列表与属性查询、本地/远端路径安全、创建/删除/移动/重命名/chmod 编排、外部拖放规划、russh/libssh SFTP session/file/metadata 统一边界、队列、批次规划、本地与 SCP 传输入口、本地断点安全、SFTP 断点和数据面、SFTP 路径安全、SSH 命令型 remote-copy 协议、远端 modem 命令启动/marker/finalize、跨协议 modem 调度与运行时读取、共享 packet codec/原子输出，以及独立 X/Y/ZModem 数据面。
   - session I/O/logging：`session_commands.rs`、`session_profile_delete.rs`、`session_terminal.rs`、`outbound_io.rs`、`outbound_events.rs`、`session_events.rs`、`system_event_sink.rs`、`sysmon_commands.rs`、`sysmon_metrics.rs`、`sysmon_network.rs`、`sysmon_network_io.rs`、`sysmon_remote_parsing.rs`、`sysmon_runtime.rs`、`terminal_export_commands.rs`、`trigger_runtime.rs`、`log_commands.rs` 和 `log_storage.rs` 承接 session open/close 生命周期、Profile 删除事务、session 查询/输入/按键编码/跨协议 resize 命令适配、每会话出站 lane、跨协议 writer、双向事件、system event notifier/后台 outbox drain/关闭收尾、Sysmon 桌面命令以及本机/远端采集限制、Linux/macOS/FreeBSD shell 命令、Windows PowerShell 探测/采样脚本、四平台 snapshot 解析/Windows JSON DTO 和网络地址回退、终端导出桌面命令适配、触发动作/受限子进程/发送批次编排、日志/归档桌面命令、append-only shard 存储与管理，以及 bytesRef v1/v2 解析、范围读取和 SHA-256 替换检测。
   - bootstrap/shared state/API types：`app_bootstrap.rs` 承接数据目录迁移、Vault/Store/IPC 初始化、Tauri command 注册和退出收尾；`state.rs` 承接 `AppState`、runtime registry、`SessionIo`、session-open cancellation/lifecycle lane、凭据操作文件锁和共享 runtime map ownership；`command_types.rs` 承接传输、文件、日志、凭据、迁移、Tmux、Host Key 等 Tauri IPC DTO 及其稳定 serde 契约，避免主文件继续持有跨 transport/security/storage 的状态和接口边界。
   - archive/bundle security：`archive_support.rs` 承接导出目录约束、原子正文/校验和、SHA-256、受限文件打开和 tar.gz 流式归档；`bundle_signing.rs` 承接 keyring/Portable Vault 签名身份 failover、read-back 核验、Ed25519 detached signature 和多产物 fail-closed 收敛；`bundle_export.rs` 承接 redaction/raw 策略、附件 TOCTOU/容量约束、manifest、tar entry 预算和最终导出编排。

   SSH channel message、stdout/stderr 记录、退出诊断、流持久化节流、断线状态转换与代际重连触发已迁入 247 行的 `ssh_reader.rs`；重连策略、最新 Profile 代际检查、失败收敛、动态等待和 runtime 安装循环已迁入 616 行的 `ssh_reconnect.rs`，`ssh_runtime.rs` 从 1,009 行降到 148 行并只保留 runtime 数据结构以及 russh/libssh 资源有界关闭。
   TCP/Telnet 的最新 Profile 比较、动态等待、重连失败/禁用收敛和 runtime 代际安装已迁入 527 行的 `tcp_reconnect.rs`；`tcp_transport.rs` 从 1,043 行降到 517 行并只保留 endpoint/socket/TLS 建立、初次 runtime 安装和 reader 数据面。
   russh Jump channel 到本地 socket 的 transport bridge、libssh Agent forwarding accept loop、16 channel 容量和 Unix agent socket 双向 pipe 已迁入 203 行的 `ssh_libssh_bridge.rs`；libssh stdout/stderr 有界轮询、exit status/signal/EOF 收敛、取消观察和阻塞 close worker 已迁入 159 行的 `ssh_libssh_channel.rs`，底层 channel 只通过克隆共享句柄的受限方法交给统一 writer；统一 channel façade、reader/writer、russh message 归一化和 exec opener 已迁入 285 行的 `ssh_channel.rs`，`ssh_backend.rs` 从 852 行降到 214 行并只保留 russh/libssh session dispatch。
   `session_commands.rs` 的 Profile 删除事务已迁入 135 行的 `session_profile_delete.rs`，终端查询/输入命令适配、按键编码、SSH/Shell/Telnet resize 与 terminal profile 尺寸提交已迁入 247 行的 `session_terminal.rs`，Profile expected/current/incoming 合并及代理 Secret 更新 helper 已归入 `profile_commands.rs`；`session_commands.rs` 从 1,174 行降到 644 行并只保留连接状态 helper、open/close 生命周期与对应命令适配。
   跨文件操作/传输、SSH 健康、Tmux、Sysmon 和 tunnel health 共用的 256 项 SSH auxiliary 容量、runtime 资源快照、handle/SFTP lease、SFTP 缓存失效和 russh/libssh SFTP setup 已迁入 203 行的 `ssh_auxiliary.rs`；SSH 命令型 remote-copy 的 channel 生命周期、空闲/总超时、进度 marker 校验和可移植 shell 命令已迁入 307 行的 `remote_copy.rs`。本地/远端变更路径、跨平台本地路径分类、filesystem root/home 保护、symlink 分量拒绝和 portable filename 已迁入 150 行的 `file_operation_paths.rs`；本地/SFTP 目录列表、属性读取、排序与时间格式化已迁入 218 行的 `file_metadata.rs`，`file_operations.rs` 从 1,055 行降到 687 行并只保留创建/删除/移动/重命名/chmod 变更编排。本地拖放目标校验、递归扫描、符号链接/非普通文件过滤、自复制拒绝和相对路径规范化已迁入 241 行的 `external_drop_planning.rs`，`file_batch.rs` 从 947 行降到 707 行并保留跨本地/SFTP 批次规划、冲突策略和任务启动。SFTP 断点匹配、上传/下载/远端复制和流式进度已迁入 512 行的 `sftp_transfer.rs`，递归目录创建、路径拼接和符号链接拒绝已迁入 120 行的 `sftp_paths.rs`；`file_transfer.rs` 从 1,538 行降到 400 行并只保留传输入口、本地复制和本地断点安全。
   SSH transport 的 libssh/russh 认证顺序、GSSAPI backend 选择、agent 前后置、显式 identity/私钥加载、password/keyboard-interactive/none fallback 与认证超时错误已迁入 712 行的 `ssh_authentication.rs`；russh handler 构造、Host Key 接受/共享锁错误和 remote-forward callback 已迁入 181 行的 `ssh_handler.rs`；直连和多跳目标建立、Jump Host endpoint/Host Key policy/凭据继承与有界清理已迁入 476 行的 `ssh_connection.rs`；已建立 runtime 的替换/安装、Connected Store commit、reader 启动、失败清理和一次性 Host Key 恢复已迁入 142 行的 `ssh_session_lifecycle.rs`；底层 socket 连接、TCP keepalive、有界 disconnect、共享 auxiliary exec channel 和终端 PTY/shell setup 已迁入 282 行的 `ssh_transport_setup.rs`；libssh 的 proxy/Jump bridge、Host Key 校验、GSSAPI/混合认证、终端 channel 和 runtime 组装已迁入 392 行的 `ssh_libssh_transport.rs`，`ssh_transport.rs` 从 2,484 行继续降到 305 行并只保留共享连接数据结构和 russh runtime 建立编排。跨认证、Host Key 扫描、文件传输和 SSH setup 共用的有界连接步骤及 direct-tcpip 超时清理已迁入 65 行的 `ssh_connection_steps.rs`；local/remote/dynamic tunnel 客户端、libssh remote-forward acceptor 与双向数据 pipe 已迁入 284 行的 `ssh_tunnel_io.rs`，远端监听探测、路由修复、自动恢复和健康事件已迁入 258 行的 `ssh_tunnel_health.rs`，运行列表、代际失效、停止补偿和 remote cancel 已迁入 183 行的 `ssh_tunnel_lifecycle.rs`，状态投影、标签、Profile/事件持久化与 forward route key 已迁入 116 行的 `ssh_tunnel_store.rs`，`ssh_tunnel.rs` 从 1,666 行降到 767 行并只保留 runtime 类型、创建/安装、恢复和请求校验。远端 Sysmon 的平台标签、Linux/macOS/FreeBSD/Windows snapshot 组装和 Windows JSON DTO 已迁入 377 行的 `sysmon_remote_parsing.rs`；OpenWrt/Linux/BSD 网络解析、kernel/hostname fallback 合并、接口排序和速率计算已迁入 669 行的 `sysmon_network.rs`，本地 Linux `/proc`/`getifaddrs` 网络采集、命令候选、容量/总 deadline 和进程组清理已迁入 404 行的 `sysmon_network_io.rs`，CPU/内存/负载/进程/磁盘解析、数值边界和 Linux 本机指标读取已迁入 368 行的 `sysmon_metrics.rs`，`sysmon_runtime.rs` 从 2,350 行降到 539 行并只保留刷新提交、本机命令执行和远端采集编排。共享核心 Store 的事件写入/系统 outbox 接口/容量维护/日志查询与发送审计已迁入 294 行的 `store/events.rs`，传输/审计/时间线/Sysmon 有界历史与加载归一化已迁入 217 行的 `store/histories.rs`，Host Key/认证成功决策与 MCP 授权迁入 87 行的 `store/security.rs`，bundle 组装与脱敏迁入 64 行的 `store/exports.rs`，Profile CRUD、Session runtime 状态、级联删除、摘要和断线健康语义已迁入 428 行的 `store/sessions.rs`；其余 notifier 状态、4,096 项有界 outbox、精确 session/event 丢弃与 clone-shared runtime 已迁入 107 行的 `store_system_events.rs`，`store.rs` 从 2,576 行降到 57 行并只保留序列化状态定义与模块 wiring，30 项 Store 回归按 events/security/sessions/histories/exports 迁入 `store/tests/` 五个领域文件，共享夹具集中在 124 行的 `store/tests/mod.rs`；公开 API、序列化格式和测试函数名不变。远端 X/Y/ZModem 自动启动、raw TTY shell、READY/DONE/FAIL marker、完成等待和原子 finalize 已迁入 395 行的 `modem_remote.rs`；X/Y/ZModem 各自的数据面已迁入 123/186/267 行的 `modem_xmodem.rs`、`modem_ymodem.rs`、`modem_zmodem.rs`，共享 packet codec、ACK/EOT handshake 和本地原子输出已迁入 426 行的 `modem_protocol.rs`。`modem_transfer.rs` 从 2,017 行降到 626 行并只保留方向判定、运行时 tap/取消、入口编排和有界字节 reader。SSH exec 的 channel 收尾/状态 grace/输出上限、tunnel 的连接上限/健康探测 deadline/跨平台监听命令、session-open 并发上限，以及凭据迁移 journal/诊断容量常量也已归还对应 owner，根模块不再反向持有这些领域限制。命令适配 ownership、连接状态/endpoint helper、SSH Host Key rejection formatter、串口线路控制/Break 事务、MCP 审计导出、日志事件 annotation/store-sync、X/Y/ZModem runtime tap、私有原子文件写入、共享 SSH exec channel close/shell quoting、Tmux control runtime、system event sink、Sysmon 采集/解析、跨域 DTO 和应用 bootstrap 边界已收敛；SFTP-only、SSH 正常/断线/故障和 TCP/Telnet 外部服务端兼容回归已分别迁入 `tests/external_sftp_compat.rs`、`tests/external_ssh_compat.rs` 和 `tests/external_tcp_telnet_compat.rs`，OpenSSH 进程、ssh-agent、混合认证 russh 服务端和延迟 SFTP 服务端等共享夹具已迁入 `tests/ssh_test_support.rs`，libssh backend 选择、混合认证回退、exec、SCP 和 remote-copy 语义回归已迁入 `tests/ssh_transport_tests.rs`，SSH reconnect、Host Key、Jump Host 配置和凭据策略回归已迁入 `tests/ssh_policy_tests.rs`，Telnet 协商、NVT、NAWS 与碎片化 runtime 回归已迁入 `tests/tcp_telnet_tests.rs`，Tmux target quoting、pane/control parser、容量和 runtime 清理回归已迁入 `tests/tmux_protocol_tests.rs`，Sysmon 四平台 parser、网络地址回退、采集 deadline 和 Store 提交语义回归已迁入 `tests/sysmon_tests.rs`，MCP grant 规范化、授权判定和持久化事务回归已迁入 `tests/mcp_grant_tests.rs`，approval 队列和 UI 不可用时的 fail-closed 回归已迁入 `tests/mcp_approval_tests.rs`，IPC framing/容量/token 和 endpoint 生命周期回归已迁入 `tests/mcp_ipc_tests.rs`，读取授权和脱敏回归已迁入 `tests/mcp_read_tests.rs`，执行审计与配置边界保留在 `tests/mcp_tests.rs`；原 1,867 行 transfer 综合测试也已按 ownership 拆为 `tests/transfer_queue_tests.rs` 的队列/状态事务、`tests/file_operation_tests.rs` 的路径与本地文件操作、`tests/transfer_io_tests.rs` 的限速/resume/modem 数据面，以及 `tests/file_batch_tests.rs` 的属性、批次和 external drop 回归。append-only log、bytesRef、UTF-8 截断、session event、system sink 和 retention 回归已迁入 `tests/session_logging_tests.rs`，trigger action、受限 shell command、send-text 容量和 stale runtime 回归已迁入 `tests/trigger_tests.rs`，log shard 管理/搜索/归档与 bundle redaction/附件/签名输入安全回归已迁入 `tests/archive_tests.rs`，Host Key 临时/持久信任、Store/Profile mirror、并发变更和 tracked transaction 回归已迁入 `tests/host_key_tests.rs`，remote-copy marker/resume/path safety 与 SCP upload/download 命令协议回归已迁入 `tests/scp_protocol_tests.rs`，tunnel 请求/Profile 容量、metrics、启动停止补偿、跨平台 listener health 与 channel failure 清理回归已迁入 `tests/tunnel_tests.rs`；拆分后仍保持完整 372 项库测试、原 Cargo 函数过滤器、20 服务端 SSH 矩阵和 18 项 socket 服务端矩阵验证，下一步继续按领域边界拆分其余大型测试。
   Modem command/finalize、ACK/EOT retry 和 marker/handshake 回归已迁入 `tests/modem_protocol_tests.rs`，静默 XModem 取消/CAN 与 transport reconnect 失败语义回归已迁入 `tests/modem_runtime_tests.rs`；Serial 连接配置、线路控制补偿、重连 Profile、capture buffer/history/export、禁用重连和 socat PTY 替换回归及专属夹具已迁入 `tests/serial_tests.rs`；Store load normalization、orphan/quarantine、mirror key 与 loaded ID repair 回归已迁入 `tests/store_normalization_tests.rs`；JSON/SQLite persistence、schema migration、snapshot CAS、journal barrier、state hash 和 load/corruption 回归已迁入 `tests/storage_tests.rs`；凭据 migration projection/diagnostic/recovery、Profile Secret 事务与 source retention 回归已迁入 `tests/migration_tests.rs`；portable-vault batch/rekey/stale-instance/ref/salt 回归已迁入 `tests/portable_vault_tests.rs`；session delete/open/close/lifecycle lane、容量和持久化补偿回归已迁入 `tests/session_lifecycle_tests.rs`；keyring 初始化、Client Identity 生命周期、Secret usage 和 OneKey 登录/提示回归及专属夹具已迁入 `tests/identity_tests.rs`；Telnet/TLS、TCP 重连/故障与共享出站 lane 回归保留在 `tests/transport_runtime_tests.rs`；OpenSSH reconnect、SFTP/SCP/tunnel、多跳、混合认证、身份顺序与 ssh-agent 端到端回归已分别迁入 `tests/openssh_reconnect_tests.rs`、`tests/openssh_transfer_tunnel_tests.rs`、`tests/openssh_jump_host_tests.rs` 和 `tests/openssh_authentication_tests.rs`；SSH auxiliary setup/timeout/exec 收尾及 SFTP/SCP/remote-copy 取消和超时回归已迁入 `tests/ssh_runtime_tests.rs`；TCP/SSH keepalive、proxy handshake/forward 和最新 TCP reconnect Profile 回归已迁入 `tests/connection_config_tests.rs`；MCP audit 与 terminal text 原子导出、校验和、路径和容量回归已迁入 `tests/export_tests.rs`；终端尺寸、Profile normalization/auth hint/活动传输约束及 proxy Secret usage/update 回归已迁入 `tests/session_profile_tests.rs`；SSH exec buffer、Sysmon query/refresh、SSH auxiliary saturation 和 Tmux mutation lease 回归已迁入 `tests/runtime_capacity_tests.rs`；legacy app identifier 数据目录迁移与双活 Store 拒绝回归已迁入 `tests/app_migration_tests.rs`；SOCKS5 loopback domain parsing、认证方法和 command rejection 回归已迁入 `tests/proxy_runtime_tests.rs`；Shell exit/fast-exit 清理与 reader start gate 回归已迁入 `tests/shell_runtime_tests.rs`，测试正文和函数过滤器保持不变。
   OneKey completion 已并入 `tests/identity_tests.rs`，MCP HTTP config 已并入 `tests/mcp_tests.rs`，停止 tunnel 的 Profile 持久化回归已并入 `tests/tunnel_tests.rs`；共享测试 harness 和子模块注册已迁入 `tests/mod.rs`，`lib.rs` 内已无直接测试正文或夹具实现。
   独立 MCP bridge 的 `crates/portmate-mcp/src/main.rs` 已从 3,894 行降到 1,126 行：1,487 行 bridge 测试正文已迁入 `tests.rs`，测试名称和过滤器保持不变；SQLite/JSON 只读 Store 加载、Profile 容量门禁、加载后历史归一化和测试 schema 夹具已迁入 `store_loader.rs`；持久化 native keyring 选择、一次性初始化、SecretRef 校验及读写已迁入 `keyring_store.rs`；HTTP token 生成/读取、Origin allow-list、Bearer/X-Token 鉴权和常量时间比较已迁入 `http_security.rs`；MCP 历史版本协商、HTTP `Content-Type`/`Accept` 媒体类型解析、quality 边界和 SSE 请求识别已迁入 `http_protocol.rs`；有界 JSON writer、JSON-RPC 超限 envelope、HTTP/CORS headers 和 SSE event/body 编码已迁入 `response_encoding.rs`；JSON-RPC request/response/error 类型、128 项 batch 上限、ID/params/notification envelope 校验和 batch response 编排已迁入 `json_rpc.rs`；桌面 IPC endpoint DTO、私有文件/Store 绑定/tokenRef 校验、loopback 连接、请求编码、响应限额和 socket 生命周期已迁入 `desktop_ipc.rs`；HTTP request framing、Content-Length/chunked 解码、header 合并、容量限制和总 deadline 已迁入 `http_request.rs`，被 HTTP 与桌面 IPC 共同使用的 socket deadline 读取则迁入 `socket_io.rs`；入口继续负责每个请求的运行时 Store/IPC 刷新、MCP method 业务编排、持续 SSE 状态生成和 server lifecycle。拆分后 36 项 bridge 单测、直接 stdio/HTTP 生命周期、TypeScript SDK 四版本和 Python SDK 八版本的 stdio/HTTP 矩阵均保持通过。
2. Bundle identifier 数据目录迁移、PortMate 自有状态判定和 Store/legacy JSON/vault 路径常量已从启动主文件提取到 `app_data_migration.rs`，保留 bootstrap-only WebView 目录替换和双活 Store fail-closed 回归。SQLite schema、mirror、load/transaction 已分别迁入 `sqlite_schema.rs`、`sqlite_mirror.rs`、`sqlite_store.rs`，大型追加表按 raw JSON 相等性增量写入并有 INSERT/UPDATE/DELETE 回归；legacy JSON load 与完整旧快照归一化已进入 `store_normalization.rs`，JSON compatibility write/Store orchestration 已进入 `store_persistence.rs`；继续评估兼容快照异步化与更细的 storage API 所有权。
3. Stronghold portable vault 已覆盖 OS keyring 不可用/禁用场景、主密码轮换、SSH/Tmux 凭据批量迁移和保守的跨重启恢复；命令层已迁入 `vault_commands.rs`，snapshot consistency 与 commit/rekey guard 已迁入 `state_snapshot.rs`，journal 持久化与读回验证已迁入 `migration_journal_store.rs`，recovery 判定与自动收敛已迁入 `migration_recovery.rs`，migration types/planning/runtime/diagnostics 已分别迁入独立模块，native keyring 与跨 provider 路由/批处理已迁入 `secret_provider.rs`，Stronghold 底层 provider 已迁入 `portable_vault.rs`。Linux 已实测 Stronghold 的 1,200 字节跨进程 CRUD、错误密码、损坏 snapshot、缺失 salt、commit rollback、stale snapshot CAS，以及 Unix `0700`/`0600`、symlink/非普通文件/多硬链接拒绝；同一跨进程矩阵已进入 Windows/macOS Native CI。Linux Secret Service 跨进程 CRUD、provider unavailable 和 locked/prompt-denied 也有真实隔离证据，Windows/macOS native keyring CRUD probe 已进入 Native CI；继续补对应平台成功记录、Windows/macOS locked/denied provider 和非 Unix Stronghold 文件系统故障证据。
4. Linux DEB/RPM/AppImage 打包与逐包检查已完成；`Native CI` 的 Ubuntu job 会在 `desktop:build` 后运行 `npm run test:linux-package`，从新生成的 DEB/RPM/AppImage 提取 sidecar 并运行全语言 MCP 生命周期矩阵。Windows job 已接入 MSI administrative extraction、NSIS 静默安装/卸载以及主程序/sidecar/LICENSE 唯一性和 SHA-256 检查；macOS job 已接入直接 app、DMG verify/只读挂载、Info.plist 和相同 payload 检查，未签名二进制必须匹配 release SHA-256，签名副本则必须通过严格 codesign 且与 DMG 副本一致。共享布局逻辑在当前 Linux 主机通过 9 项夹具回归，但两个原生检查仍需对应平台的实际 CI 成功证据；签名和 Apple notarization 也仍是发布前置项。
5. `RELEASE.md` 已建立版本、测试、签名、权限、迁移、产物校验、发布和回滚 gate；正式发布前仍需在三平台逐项执行并记录证据。

## 建议的近期执行顺序

1. 集成测试环境加入真实 FreeBSD/macOS SSH tunnel 主机；跨平台探测命令与解析单元矩阵已完成。
2. keyring/Stronghold 的 Windows/macOS/Linux 故障注入矩阵；Linux Secret Service 跨进程 CRUD、provider unavailable 和 locked/prompt-denied，以及 Linux Stronghold 跨进程 CRUD/CAS、错误密码、损坏/缺失文件、commit rollback 和 Unix 私有文件边界均已实测，Windows/macOS 两类 CRUD probe 已接入 CI，durable migration journal、异常提交核对、重载 UX、双向迁移和 conflict 诊断导出已完成；仍需 Windows/macOS runner 成功、locked/denied provider 和对应文件系统故障证据。
3. SSH 三层健康与服务端故障矩阵已完成；继续扩展跨平台传输、Serial 物理设备与远端 OS 故障矩阵。
4. vttest、真实 tmux、四发行版全屏程序和全部浏览器 Playwright 回归已完成；继续收集三平台 native CI/安装 smoke test 证据。

这个顺序优先补“真实终端工具的可靠性”和“会话控制的安全边界”，比继续堆 UI 设置项更能降低后续返工。
